[build]
builder = "nixpacks"
buildCommand = "cd frontend && npm ci && npx vite build && cd .. && go build -o server ./cmd/server"

[deploy]
startCommand = "./server"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
