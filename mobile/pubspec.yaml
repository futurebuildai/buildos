name: futurebuild_field
description: FutureBuild OS Field Portal — offline-first mobile app for field workers
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.6.0
  flutter: ^3.27.0

dependencies:
  flutter:
    sdk: flutter
  drift: ^2.22.0
  sqlite3_flutter_libs: ^0.5.0
  path_provider: ^2.1.0
  path: ^1.9.0
  http: ^1.2.0
  connectivity_plus: ^6.1.0
  workmanager: ^0.5.2
  shared_preferences: ^2.3.0
  uuid: ^4.5.0
  intl: ^0.19.0
  provider: ^6.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  drift_dev: ^2.22.0
  build_runner: ^2.4.0
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
