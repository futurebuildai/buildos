# HANDOFF — current state of play

This file is the per-session living state of the BuildOS branch. Update
it at the end of every working session so the next session (different
workstation, different Claude instance, you on Monday morning) lands
with full context in 60 seconds.

**Update protocol:** at the end of a session, edit the four sections
below: "Last shipped", "In flight", "Blocked", "Next up". Keep prose
short — bullet lists, not essays. Anchor to commit SHAs and PR numbers.

Companion docs:
- [CLAUDE.md](./CLAUDE.md) — architecture + conventions (slow-changing)
- [.agents/handoff/ADR-001](./.agents/handoff/ADR-001-vision-alignment.md), [ADR-002](./.agents/handoff/ADR-002-single-tenant-fork-model.md) — strategic decisions
- [.agents/handoff/PRODUCTION_READINESS_PLAN.md](./.agents/handoff/PRODUCTION_READINESS_PLAN.md) — phase plan
- [.agents/handoff/NEXT_STEPS.md](./.agents/handoff/NEXT_STEPS.md) — prioritized backlog with concrete entry points
- [docs/fork-onboarding.md](./docs/fork-onboarding.md) — per-customer fork provisioning runbook

---

> **⚠️ ARCHITECTURE PIVOT (2026-06-01).** BuildOS is now a **self-contained
> standalone deployment**. "The Brain" hub-and-spoke model is paused: auth,
> AI, 3rd-party credentials, and billing are all native and admin-configurable
> inside the fork now. Many "Last shipped" entries below (PRs #13-#26 — Brain
> client, Maestro envelopes, A2A emitter/receiver, billing) describe code that
> has since been **removed**. They're retained as a historical record. The
> current architecture is in [CLAUDE.md](./CLAUDE.md) and
> [.agents/handoff/API_CONTRACT.md](./.agents/handoff/API_CONTRACT.md). The
> "Cross-repo coordination" section below is obsolete and marked as such.

## Last shipped (most recent → older)

- **2026-06-02** [`<pending>` → `origin/main`] **DB-backed integration tests for the whole `FieldService` write surface — `Sync` 14.8% → 85.2%, `ReportProgress` 38.7% → 93.5%, `Checkin` 31.0% → 82.8%, `DailyLog` 34.3% → 82.9% (test-only, zero production change). Second Tier-1b cluster off the NEXT_STEPS frontier.** The mobile `/api/v1/field/*` surface read ~15-39% because the existing `field_test.go` units stop at the input guards — no test drove the tx bodies (user lookup → org-scope verify → store write → audit) against a real pool. New **`internal/service/field_integration_test.go`** (`//go:build integration`) with a `newFieldServiceFixture` helper (pool + org/user/project seed; the user's OIDC subject is its id string per the post-pivot `testdb.SeedUser`) + a `capturingAuditRecorder`: (1) **Sync** — happy path returns the caller's open assigned task + a non-zero `server_time` + a non-nil (role-set) feed-card slice; plus the nil-org guard and the **unknown-subject → `ErrNotFound`** leg. (2) **ReportProgress** — writes the `task_progress` row, asserts the `field.task_progress.reported` audit entry, then a **re-submit with the same idempotency key → `ErrIdempotencyConflict`** (the 409 path) and a **task in another org → `ErrNotFound`** (the `VerifyTaskInOrg` leg). (3) **Checkin** — writes the `crew_checkins` row + idempotency-conflict re-submit. (4) **DailyLog** — writes the `daily_logs` row + the cross-org **`VerifyProjectInOrg` → `ErrNotFound`** leg. (Cross-org cases seed the foreign org first to satisfy the `projects_org_id_fkey` FK.) Remaining uncovered legs are the generic store-error `fmt.Errorf` wraps. Verify green: gofmt clean, `go vet -tags=integration ./internal/service/`, `go test -tags=integration ./internal/service/ -run TestFieldService` (~4.8s incl. container), `go build ./...` + `-tags=prod`, `make audit` ALL PASSED. **Coverage sweep status:** Tier-1b service-mutation lift continuing — `feed.go` (DismissCard/ActionCard/ListFeed ~13-41%) and the `agents.go` AI-driven paths (GenerateDailyBriefing/RecommendScheduleAdjustments ~11-14%) are the next clusters.
- **2026-06-02** [`ca8b331` → `origin/main`] **Service-layer integration tests for `HRService` — `ListEmployees` 16.7% → 83.3% and `ListCertifications` 12.5% → 87.5% (`NewHRService` 0% → 100%; test-only, zero production change). First item off the Tier-1b "DB-backed service mutations" frontier in NEXT_STEPS.** The store-level ordering/isolation is already proven in `store/hr_integration_test.go`, but the thin read-only-tx **service wrappers** read ~15% because no test drove them against a real pool (the unit tests stop at the nil-org guard). New **`internal/service/hr_integration_test.go`** (`//go:build integration`, inline `seedHREmployee`/`seedHRCert` helpers, `store.NewHRStore()`): (1) **ListEmployees** — happy path asserts strict org isolation (orgA's 2 rows, not orgB's) in directory order (last_name ASC), plus the `uuid.Nil` caller-org guard → `ErrInvalidInput`. (2) **ListCertifications** — happy path (soonest-expiring first), the `uuid.Nil` employee-id guard → `ErrInvalidInput`, and **most importantly the cross-org leg**: orgB asking for orgA's employee gets the store's `VerifyEmployeeInOrg` failure **translated to the service-level `ErrEmployeeNotFound`** (never leaks existence across tenants). Remaining uncovered legs are the generic store-error `fmt.Errorf` wraps (non-cross-org DB faults — not deterministically triggerable). Verify green: gofmt clean, `go vet -tags=integration ./internal/service/`, `go test -tags=integration ./internal/service/ -run TestHRService` (~2.6s incl. container), `go build ./...` + `-tags=prod`, `make audit` ALL PASSED. **Coverage sweep status:** library-package 0%-function sweep is complete (`internal/ai`/`cryptobox`/`mailer` all clean as of `8d60beb`); now lifting the **partial** service-mutation branches per NEXT_STEPS Tier-1b — `field.go` (Sync/ReportProgress/Checkin/DailyLog ~15-39%), `feed.go` (DismissCard/ActionCard/ListFeed), and the `agents.go` AI-driven paths are the next clusters.

- **2026-06-02** [`8d60beb` → `origin/main`] **Closed the last 0%-covered function in `internal/ai` — `(*HTTPError).Is` 0% → 100% (package total now 88.0%; test-only, zero production change).** `HTTPError.Is` is the status-code → sentinel mapping that lets callers write `errors.Is(err, ai.ErrRateLimited)` / `ErrTransient` against a typed `*HTTPError` without a type assertion. It read 0% even though `resilience_test.go` does `errors.Is(err, ErrTransient)` — because the transport returns the **bare sentinel** after exhausting its retry budget, so `errors.Is` short-circuits on identity before ever dispatching to the method. New Docker-free **`internal/ai/types_test.go`** (`TestHTTPError_Is`, white-box `package ai`) tables five statuses through the real `errors.Is` dispatch: `429` → `ErrRateLimited` only, `500`/`503` → `ErrTransient` only (the `>= 500` leg), `400`/`404` → neither, and a sentinel the method doesn't switch on (`ErrCircuitOpen`) never matches. Verify green: gofmt clean, `go vet ./internal/ai/`, `go test ./internal/ai/` (88.0%, no 0% funcs left), `go build ./...` + `-tags=prod`, `make audit` ALL PASSED. **Coverage sweep status:** `internal/ai`, `internal/cryptobox`, and `internal/mailer` are now all fully swept (no 0% funcs). The only remaining real 0% targets are the `cmd/*` process entrypoints (`main`/`run` in server/worker/fork-init, bench-gate) which need a live boot.

- **2026-06-02** [`eac2f3f` → `origin/main`] **First tests for `cmd/migrate` — the SQL migration runner went from untested to `extractVersion` 80% / `runAppMigrationsUp` 67.7% / `runAppMigrationsDown` 64.3% (package total 0% → 42.6%; test-only, zero production change). First coverage outside `internal/`.** `cmd/migrate` is the third binary (applies River's internal migrations then `migrations/NNN_*.up.sql`/`.down.sql` against `schema_migrations`) and had no tests. Two new files: (1) **`cmd/migrate/main_test.go`** (Docker-free unit) — `TestExtractVersion` tables five paths through the pure `extractVersion` helper (`migrations/001_initial_schema.up.sql` → `001`, abs paths, a bare `900_…` name, and the no-underscore fallback that returns the input unchanged). (2) **`cmd/migrate/main_integration_test.go`** (`//go:build integration`) — `TestRunAppMigrationsUpDown` drives the real runner end-to-end against a `testdb.NewPool` container using a `t.TempDir()` of **throwaway version-900 migration files** (`900_widget.up.sql` creates a disposable `mig_test_widget` table, `.down.sql` drops it) so it never collides with the repo's real migrations: the **up** path creates the `schema_migrations` tracking table + globs + applies + records version 900 (asserted via `to_regclass` + a `schema_migrations` count), a **re-run** proves the already-applied `if exists { continue }` idempotent skip (still exactly one row), and the **down** path reverse-globs + applies + de-registers (table gone, zero rows). Helpers `writeFile`/`tableExists`/`versionCount` take `*pgxpool.Pool` directly. Verify green: gofmt clean, `go vet ./cmd/migrate/` + `-tags=integration`, `go test ./cmd/migrate/ -run TestExtractVersion` + `go test -tags=integration ./cmd/migrate/ -run TestRunAppMigrationsUpDown` (~1.4s incl. container), `go build ./...` + `-tags=prod`, `make audit` ALL PASSED. **Coverage sweep status:** `internal/` 0%-function sweep stays complete (only the cosmetic empty-body `NoopAuditRecorder.Record` artifact remains); remaining real 0% targets are the `cmd/*` process entrypoints (`main`/`run` in server/worker/migrate/fork-init, bench-gate) which need a live boot to exercise.

- **2026-06-02** [`d3cb113` → `origin/main`] **Closed the last meaningful 0%-covered function across `internal/` — `config.Load` + `PhysicsConfig.WithDefaults` 0% → 100%, and the four `getEnv*` scalar parsers 50% → 100% (config package 83.7% → 93.5%; test-only, zero production change). The `internal/` 0%-function sweep is now effectively COMPLETE.** New Docker-free **`internal/config/config_test.go`** uses `t.Setenv` (auto-restored) for hermetic env control: (1) **WithDefaults** — a zero `PhysicsConfig` gets the CPM defaults (2000 SF / 0.35 exponent), explicit non-zero values are preserved; (2) **Load_HappyPathFromEnv** — empty `CONFIG_SOURCE` → `EnvSecretSource`, `DATABASE_URL` present → full `Config` with the documented scalar defaults (`PORT=8080`, `DB_POOL_MAX=25`); (3) **Load_UnknownConfigSourceErrors** — a malformed `CONFIG_SOURCE` (`bogus:nope`) fails fast (LoadSecretSource rejects unknown prefixes) so Load never returns a partial Config; (4) **LoadWithSource_ParsesScalarEnv** — valid non-default `DB_POOL_MAX=42` / `DB_TIMEOUT=9s` / `OTEL_EXPORTER_OTLP_INSECURE=true` / `SENTRY_TRACES_SAMPLE_RATE=0.25` drive the getEnvInt/Duration/Bool/Float **success** branches (the previously-uncovered halves); (5) **LoadWithSource_MissingDatabaseURLErrors** — the one hard-required secret absent → error. Verify green: gofmt clean, `go vet ./internal/config/`, `go test ./internal/config/`, `go build ./...` + `-tags=prod`, `make audit` ALL PASSED. **Coverage sweep status:** the only line still reading 0.0% anywhere in `internal/` is `NoopAuditRecorder.Record` — an **empty-body func with zero instrumentable statements** (the cover tool's permanent NaN→0.0% artifact; the no-panic test from the prior increment exercises it). The 0%-function hardening pass across the backend is DONE; future coverage lifts are partial-branch fills, not new-function closures.

- **2026-06-02** [`345dc4b` → `origin/main`] **Closed the last real 0%-covered functions in `internal/service` — `recordDailyBriefingAudit` 0% → 83.3% and `AuditService.Record`'s programmer-error guard branches now covered (test-only, zero production change).** Two new test files: (1) **`internal/service/agents_integration_test.go`** (`//go:build integration`) — `TestAgentsService_RecordDailyBriefingAudit_WritesEntry` calls the private `recordDailyBriefingAudit` directly against a real pool (the unit tests in `agents_test.go` pass a nil pool and stop at the AI boundary, so the standalone audit tx read 0%) with a `capturingAuditRecorder`, asserting the entry's labels (`ai.daily_briefing.invoked` / `AuditResourceAIRun` / `ResourceID == SessionID` / org + sub) and the metadata JSON shape (`session_id`, `task_count`, `alert_count`, `task:"daily_briefing"`). (2) **`internal/service/audit_test.go`** (Docker-free unit) — `TestNoopAuditRecorder_RecordIsNoOp` (the no-op stand-in neither panics nor dereferences its nil tx) + `TestAuditService_Record_InvalidEntrySkips` (table of the four missing-field cases — nil org / empty action / empty resource type / nil resource id — each logged at WARN and dropped before `InsertAudit`, so a nil tx is never touched). **Note:** `NoopAuditRecorder.Record` still prints 0.0% in the cover report — that's an **unfixable cosmetic artifact** (an empty-body func has zero instrumentable statements, so the tool always reports 0.0% no matter how often it's called; the test still asserts no-panic). Verify green: gofmt clean, `go vet ./internal/service/` + `-tags=integration`, `go test ./internal/service/` + `go test -tags=integration ./internal/service/`, `go build ./...` + `-tags=prod`, `make audit` ALL PASSED. **Coverage sweep status:** the only meaningful 0% func left across `internal/` is the big `config.Load` env loader.

- **2026-06-02** [`8ac12b2` → `origin/main`] **🐛 Boot-bricking bug found + fixed via coverage hardening: `service.SeedBootstrapTokenIfNeeded` went 0% → 85.0% and the new test caught that a fork with `BUILDOS_BOOTSTRAP_TOKEN` set boots once then FAILS TO START on every restart.** Root cause: the idempotent re-boot branch (SQLSTATE `23505` on `UNIQUE(token_hash)`) returned `nil` from the `pgx.BeginTxFunc` closure → told pgx to **COMMIT a tx the failed INSERT had already poisoned** → commit surfaced as `commit unexpectedly resulted in rollback`. `cmd/server` treats that non-nil error as **fatal** (`main.go:207-209`), so the documented "idempotent — re-boots are a silent no-op" contract (`main.go:202-203`) was violated and any container restart / redeploy with the token still in env bricked the fork. **Fix (production, `internal/service/setup.go`):** the `23505` branch now returns a package-private `errBootstrapTokenExists` sentinel so BeginTxFunc **rolls the poisoned tx back cleanly**, and the caller translates that sentinel to the documented `(false, nil)`. **Tests (`internal/service/setup_integration_test.go`, `//go:build integration`):** added `validBootstrapCleartext` helper + four cases — (1) **ValidationShortCircuits**: empty → `(false,nil)`, wrong length + non-base64url → `ErrInvalidInput` (all before any DB work); (2) **SeedsAndIsIdempotent**: first seed (ttl=0 → DefaultBootstrapTokenTTL) lands a row + a `system:boot` issue audit entry, the **re-seed is now a clean `(false,nil)` no-op with no new audit row** (the regression guard), and the seeded cleartext is genuinely redeemable; (3) **NilOrgPicksIncompleteOrg**: `uuid.Nil` resolves the single onboarding-incomplete org (proven by redeem); (4) **CompleteOrgIsNoOp**: explicit complete org + the `uuid.Nil` all-onboarded `ErrNoRows` path both no-op. Verify green: gofmt clean, `go vet ./internal/service/` + `-tags=integration`, `go test -tags=integration ./internal/service/ -run TestSetupService` (full wizard suite, ~31s), `go build ./...` + `-tags=prod`, `make audit` ALL PASSED. **Coverage sweep status:** remaining 0% funcs are `service.recordDailyBriefingAudit`, the `audit.Record` closure, and the big `config.Load` env loader.

- **2026-06-02** [`170b80e` → `origin/main`] **Docker-free unit tests for the three 0%-covered observability wrappers — `obs.ObserveAICall`, `obs.SentryHTTPMiddleware`, `obs.CaptureMessage` all 0% → 100% (test-only, zero production change, no new dependency).** These are the egress/telemetry seams that fire only under real traffic, so they read 0%. Extended **`internal/obs/metrics_test.go`** + **`internal/obs/sentry_test.go`**: (1) **ObserveAICall** — a success + an error call on one `(kind, model)` then **scrapes the `/metrics` handler** (matching the file's existing `strings.Contains` style rather than pulling in `prometheus/testutil`, which would add a `kylelemons/godebug` indirect dep) and asserts `buildos_ai_requests_total{…,outcome="success"} 1` + `outcome="error" 1` (outcome derived from `err`) and the duration histogram `_count … 2`. (2) **SentryHTTPMiddleware** — builds the middleware and runs a request through it, asserting the wrapped handler executes and its `418` response is preserved (the per-request hub scope never alters a non-panicking response; `Repanic=true`). (3) **CaptureMessage** — the empty-message guard short-circuits before any hub work, and a non-empty message + tags against an **uninitialized SDK** (no-op default hub) does not panic (mirrors the existing `CaptureError` smoke test). Verify green: gofmt clean, `go vet ./internal/obs/`, `go test ./internal/obs/`, `go build ./...` + `-tags=prod`, **`go.mod`/`go.sum` unchanged**, `make audit` ALL PASSED. **Coverage sweep status:** remaining 0% funcs are the DB-touching `service.recordDailyBriefingAudit` / `audit.Record` closure / `setup.SeedBootstrapTokenIfNeeded`, and the big `config.Load` env loader.

- **2026-06-02** [`c295a9e` → `origin/main`] **Docker-free unit tests closing five pure 0%-covered helpers across four packages — `currency.Zero`, `models.IsValidReportedVia`, `models.IsValidCertificationStatus`, `mailer.WithLogger`, and `middleware.IsProdBuild` all 0% → 100% (test-only, zero production change).** A cross-package `coverpkg` survey (after the store/worker/api 0%-leg sweep) surfaced these pure functions as the highest-ROI remaining gaps — no Docker, no infra, deterministic. Added/extended: (1) **`internal/currency/currency_test.go`** `TestZero` — `Zero(USD)` yields `{0, USD}` and an unsupported code propagates `New`'s validation error. (2) **`internal/models/validators_test.go`** (new) — `IsValidReportedVia` (web/mobile accepted, case-sensitive rejects) + `IsValidCertificationStatus` (active/expired/revoked accepted, others rejected). (3) **`internal/mailer/resend_test.go`** `TestWithLoggerOption` — the `WithLogger` functional option installs the supplied `slog.Logger`, and the default stays non-nil (no nil-panic on log). (4) **`internal/api/middleware/buildmode_dev_test.go`** (new, `//go:build !prod`) — `IsProdBuild()==false` in a dev build, plus **`buildmode_prod_test.go`** (new, `//go:build prod`) — `IsProdBuild()==true` under `-tags=prod`, so both D8 build-mode variants are asserted (`make test` + `make test-prod`). Verify green: gofmt clean, `go vet ./internal/...`, `go test ./internal/currency/ ./internal/models/ ./internal/mailer/ ./internal/api/middleware/`, `go test -tags=prod ./internal/api/middleware/ -run TestIsProdBuild`, `go build ./...` + `-tags=prod`, `make audit` ALL PASSED. **Coverage sweep status:** remaining 0% funcs are the DB-touching ones (`service.recordDailyBriefingAudit`, the `audit.Record` closure, `setup.SeedBootstrapTokenIfNeeded`), the big `config.Load` env loader, and the observability wrappers (`obs.ObserveAICall`, `obs.SentryHTTPMiddleware`, `obs.CaptureMessage`).

- **2026-06-02** [`b2e2e90` → `origin/main`] **DB-backed integration test for `worker.Registry.Start` — the River worker daemon's boot path went 0% → 100%, closing the last 0%-covered function in the `internal/worker` package (worker total now 91.0%, up from 88.5%; test-only, zero production change).** `Registry.Start` is the entrypoint `cmd/worker` blocks on, but read 0% because no test had ever booted the client against a live River schema. Extended **`internal/worker/registry_integration_test.go`** with **TestRegistry_Start**: applies **River's own migrations** via `rivermigrate` (the `river_job` tables aren't in `migrations/*.up.sql` — `cmd/migrate` applies them separately), builds the registry with the existing `fakeRunner`/`fakeDeliverer`/`fakeChecker` deps, then **boots the client** (`Start(ctx)` is non-blocking — it spins up the producers and returns nil once running) and **gracefully drains** it via `Client.Stop` with a bounded ctx (mirroring `cmd/worker`'s shutdown). A missing River schema or bad client config would surface as a non-nil `Start` error. Verify green: gofmt clean, `go vet -tags=integration ./internal/worker/`, `go test -tags=integration ./internal/worker/ -run TestRegistry_Start` (~1.4s incl. container + River migrate), `go build ./...` + `-tags=prod`, `make audit` ALL PASSED. **Coverage sweep status:** `internal/store`, `internal/worker`, and `internal/api` now all have **zero** 0%-covered functions (totals 81.2% / 91.0% / 87.1%). The 0%-leg sweep across the core backend packages is complete — next lifts are partial-coverage branch fills (error-wrap legs) or moving to the as-yet-unswept packages (`internal/ai`, `internal/cryptobox`, `internal/mailer`, `cmd/*`).

- **2026-06-02** [`6a1fdec` → `origin/main`] **Docker-free unit tests for `store.NewPool`'s two error branches — `NewPool` 0% → 89.5%, closing the last 0%-covered function in the `internal/store` package (store total now 81.2%, up from 62.8% at the start of this coverage sweep; test-only, zero production change).** `NewPool` is exercised at runtime on every `cmd/server`/`cmd/worker` boot but read 0% in the coverage profile (`testdb.NewPool` builds its own pool via `pgxpool.New`, not `store.NewPool`). New **`internal/store/pool_test.go`** (no build tag → runs under `make test`, no Docker) covers the two error returns deterministically: (1) **InvalidDSN** — a DSN with a non-numeric port fails `pgxpool.ParseConfig`, so `NewPool` returns before any network I/O. (2) **UnreachableDB** — a well-formed DSN pointing at `127.0.0.1:1` (a reserved port nothing listens on) parses + builds the lazy pool, then the first `Ping` is refused (immediate `ECONNREFUSED`, no timeout wait), so `NewPool` closes the pool and returns the ping error. Only the trailing `return pool, nil` happy line stays uncovered (it needs a live DB; the success path is exercised end-to-end by every integration test's container anyway). Verify green: gofmt clean, `go vet ./internal/store/`, `go test ./internal/store/ -run TestNewPool` (both pass in ~3ms, no Docker), `go build ./...` + `-tags=prod`, `make audit` ALL PASSED (the new tests run inside `make test`). **Coverage sweep status:** the `internal/store` package now has **zero** 0%-covered functions. Next lifts move up the stack — `internal/worker` job handlers' live-DB paths and `internal/api` handler-level integration.

- **2026-06-02** [`88937f1` → `origin/main`] **DB-backed store-layer integration tests for the two 0%-covered `ProcurementStore` AI-recommend legs — `GetProcurementItem` 0% → 85.7%, `CreateProcurementRecommendation` 0% → 80.0% against a real Postgres (test-only, zero production change).** Closed the last `procurement.go` store gaps from the 0%-leg sweep (`56f37a3` financials, `5be05ab` pipeline, `7ed253e` schedule) — both legs back `ProcurementService.RecommendVendors` (confirm ownership + pull budget context → persist the ranked batch) but were exercised only via the service binary, so the store profile read 0%. Extended **`internal/store/procurement_integration_test.go`** with two tests. Coverage: (1) **GetProcurementItem** — a matching `(id, org)` returns the row with its budget context (`estimated_cost_cents`/`currency` for the AI prompt), while both an **org mismatch and an unknown id collapse to `ErrProcurementItemNotFound`** (no row-existence leak). (2) **CreateProcurementRecommendation** — a two-row batch sharing one **`RunID`** commits together: row 1 carries a known `VendorID` + `Reasoning` (both round-trip), row 2 leaves the **nullable `VendorID`/`Reasoning` nil** (Maestro may name a vendor with no BuildOS record yet), confidence/spend pairs persist, and a direct `count(*) WHERE run_id` confirms both landed under the shared run (the replay key). Verify green: gofmt clean, `go vet -tags=integration`, `go test -tags=integration ./internal/store/ -run 'TestProcurementStore_(GetProcurementItem|CreateProcurementRecommendation)'` (~2.5s incl. container), `go build ./...` + `-tags=prod`, `make audit` ALL PASSED (backup-db-test 6/6). **Next coverage candidates:** `pool.go` (`NewPool` — needs a real DSN, already exercised implicitly by every integration test's `testdb.NewPool` but reads 0% in the unit profile); otherwise the store layer's 0%-leg sweep is essentially closed — next lifts move to `internal/worker` job handlers' live-DB paths and `internal/api` handler-level integration.

- **2026-06-02** [`7ed253e` → `origin/main`] **DB-backed store-layer integration tests for the four 0%-covered `ScheduleStore` read/CPM-writeback queries — `GetProjectTasks` 0% → 83.3%, `GetProjectDependencies` 0% → 85.7%, `GetProjectStartDate` 0% → 71.4%, `UpdateSchedule` 0% → 87.5% against a real Postgres (test-only, zero production change).** Continued the store-layer 0%-leg sweep (`56f37a3` financials, `5be05ab` pipeline) on the schedule store — these legs back the canonical CPM recalc (load DAG → physics → write-back) but were only exercised via the service binary, so the store profile read 0%. Extended **`internal/store/schedule_integration_test.go`** with a `seedDependency` helper (`time`+`models` imports) and three tests. Coverage: (1) **GetProjectTasksAndDependencies** — two tasks inserted out of WBS order come back **WBS-ordered** (`1.0` before `2.0`, proving the `ORDER BY`), and the single `FS` dependency wiring round-trips (`PredecessorID`/`SuccessorID`/`DependencyType`). (2) **GetProjectStartDate** — the COALESCE anchor: a **pinned `project_start_date` wins**, and a project with no pinned/permit date **falls back to `created_at`** (never zero). (3) **UpdateSchedule_PersistsCPMFields** — proves the CPM write-back skips tasks absent from the results map via the `ok` guard: a `computed` task's `early_start`/`is_critical`/`total_float` round-trip through `GetProjectTasks` (the model fields are `*time.Time`/`*int` pointers — verified non-nil + value), while an `untouched` task **absent from the map keeps its NULL schedule fields** (`EarlyStart == nil`). Verify green: gofmt clean, `go vet -tags=integration`, `go test -tags=integration ./internal/store/ -run TestScheduleStore` (~11s incl. containers), `go build ./...` + `-tags=prod`, `make audit` ALL PASSED (CPM80=188µs / CPM200=489µs within gate; backup-db-test 6/6). **Next coverage candidates:** the last 0%-store read legs — `procurement.go` (`GetProcurementItem`/`CreateProcurementRecommendation`), `pool.go` (`NewPool`).

- **2026-06-02** [`5be05ab` → `origin/main`] **DB-backed store-layer integration tests for the seven 0%-covered `PipelineStore` read/list/transition queries — `ListProspects`/`UpdateProspect`/`MarkProspectPermitIssued` 0% → 88.9%, `ListEstimatesForProspect`/`ListPermitsForProspect`/`ListPipelineAnalytics` 0% → 81.8%, `GetPermit` 0% → 85.7% against a real Postgres (test-only, zero production change).** Continued the store-layer 0%-leg sweep (`56f37a3` financials) on the pipeline store — the read/list/analytics legs were exercised only via the service binary, so the store profile read 0%. Extended **`internal/store/pipeline_integration_test.go`** with an `mkProspect` tx-scoped helper and five tests. Coverage: (1) **ListProspects_FilterAndPaginate** — 3 prospects (one advanced to QUALIFIED): unfiltered `Total=3`, the `Stage=QUALIFIED` filter narrows to 1, and `PerPage=2` caps the page with `TotalPages=2` (window-function `COUNT(*) OVER()` total stays consistent with the page). (2) **UpdateProspect_PartialAndCrossOrg** — a name+notes COALESCE patch round-trips while **`pipeline_stage` stays `LEAD`** (transitions go through AdvanceStage/MarkLost, never UpdateProspect), and a cross-org patch → `ErrNotFound`. (3) **ListEstimatesAndPermits** — 2 estimates list **newest-version-first**, 2 permits list, and `GetPermit` resolves by `(id, prospect_id)` then returns `ErrNotFound` for a wrong prospect (the ownership scoping). (4) **MarkProspectPermitIssued_LinksProject** — the Kanban→CPM link write sets stage `PERMIT_ISSUED` + probability 100 + `project_id` to the freshly-created project (same tx), cross-org → `ErrNotFound`. (5) **ListPipelineAnalytics_WeightsAndExcludes** — the LATERAL latest-estimate join: a QUALIFIED USD prospect (1M × 25% = 250k weighted) and a LEAD CAD prospect (500k × 10% = 50k) each form a currency bucket, while a **LOST prospect (9M) is excluded** (`pipeline_stage <> 'LOST'`) and an **estimate-less prospect is excluded** (INNER JOIN LATERAL) — proving the 9M never leaks into the USD bucket. Verify green: gofmt clean, `go vet -tags=integration`, `go test -tags=integration ./internal/store/ -run TestPipelineStore` (13 tests pass, ~15s incl. containers), `go build ./...` + `-tags=prod`, `make audit` ALL PASSED (backup-db-test 6/6). **Next coverage candidates:** the remaining 0%-store read legs — `schedule.go` (`GetProjectTasks`/`GetProjectDependencies`/`GetProjectStartDate`/`UpdateSchedule`), `procurement.go` (`GetProcurementItem`/`CreateProcurementRecommendation`).

- **2026-06-02** [`56f37a3` → `origin/main`] **DB-backed store-layer integration tests for the five 0%-covered `FinancialsStore` read/rollup queries — `ListCorporateBudgets`/`ListProjectFinancials`/`ListLatestARAgingSnapshots` 0% → 88.2%, `UpsertCorporateRollupForQuarter` 0% → 75%, `UpdateInvoice` 0% → 85.7% against a real Postgres (test-only, zero production change).** These store queries back the financials summary endpoints but had only their write/empty-list legs covered at the store layer (the service tests exercise them in a different binary, so the store profile read 0%). Extended **`internal/store/financials_integration_test.go`** with three seed helpers (`seedBudgetPhase` — one `project_budgets` row with all three monetary pairs sharing a currency to satisfy `chk_budget_currency_match`; `seedARSnapshot`; `setProjectStatus` — flips a seeded project off the hardcoded `active`) and four tests. Coverage: (1) **ListProjectFinancials** — two USD phases on one project sum correctly (`PhaseCount=2`, estimated 300k, committed 130k), a CAD project forms a separate bucket, a **budget-less project is omitted** by the INNER JOIN, and the currency filter narrows to one row. (2) **CorporateRollupAndList** — drives the write path (`UpsertCorporateRollupForQuarter`) and read path (`ListCorporateBudgets`) together: active + completed projects contribute per-currency buckets, an **archived project's 999k is excluded** (status guard), the USD bucket totals 150k with `project_count=2`, a `""` filter returns both USD+CAD buckets, and a **re-run upserts in place** (still 2 rows, not 4 — proves the `ON CONFLICT` key). (3) **ListLatestARAgingSnapshots** — `DISTINCT ON (currency_code)` returns the **newest** snapshot per currency (the 99k April USD row wins over the 10k January one), currency filter narrows to CAD. (4) **UpdateInvoice** — `status`/`paid_date` COALESCE patch round-trips while `amount_cents` is preserved (not in the patch), and a missing id → `ErrNotFound`. Verify green: gofmt clean, `go vet -tags=integration`, `go test -tags=integration ./internal/store/ -run TestFinancialsStore` (5 tests pass, ~7s incl. container), `go build ./...` + `-tags=prod`, `make audit` ALL PASSED (backup-db-test 6/6). **Next coverage candidates:** the remaining 0%-store read/list legs — `pipeline.go` (`ListProspects`/`UpdateProspect`/`ListEstimatesForProspect`/`ListPermitsForProspect`/`ListPipelineAnalytics`/`GetPermit`), `schedule.go` (`GetProjectTasks`/`GetProjectDependencies`/`UpdateSchedule`), `procurement.go` (`GetProcurementItem`/`CreateProcurementRecommendation`).

- **2026-06-02** [`be85f3f` → `origin/main`] **DB-backed integration tests for the `NotificationDeliveryService` dead-letter path + a `LoggingSender` unit test — `DeliverNotification` 0% → 100%, `recordDLQAndError` 0% → 83.3%, `loggingSender.Send`/`NewLoggingSender` 0% → covered (test-only, zero production change).** Closed the final-attempt DLQ branch the unit-test author explicitly deferred ("For final-attempt tests we'd need a real DB; those live in the integration suite"). New **`internal/service/notification_integration_test.go`** (`//go:build integration`) wires a real `NotificationDeliveryService` through a `newNotificationService(t, sender)` helper that pairs a fresh migrated pool with a **real `store.NewNotificationsStore()`** so the final-attempt write actually lands in `field_notification_dlq` (the path the nil-pool unit tests skip); `sender` is supplied per-test (reusing the default-tag `scriptedSender`/`quietLogger`) and users are seeded per-test because the DLQ `user_id` FK requires an existing row. A `dlqCount` helper asserts directly against `field_notification_dlq`. Coverage: (1) **FinalAttempt_WritesDLQAndReturnsError** — a sender failing on the **final** attempt drives `Deliver` into `recordDLQAndError`, which opens its OWN tx and persists the discard, then **still returns the transport error** so River marks the job discarded; the persisted row carries `retry_count == MaxNotificationAttempts (6)`, the stringified `last_error`, and `notification_type == sms`. (2) **NonFinalFailure_NoDLQ** — a failure BEFORE the final attempt returns the error (River reschedules) but writes **zero** DLQ rows, even with a real pool wired. (3) **Success_NoDLQ** — a successful send (even on the final attempt) returns nil and never touches the DLQ. (4) **DeliverNotification_AdapterWritesDLQ** — exercises the worker-boundary adapter (`worker.NotificationDeliverer` impl): a `workerRetryArgs` payload mapped through `DeliverNotification` into the typed `Deliver` call, so a final-attempt failure through this entrypoint also persists a DLQ row. Plus **`notification_test.go`** gains **TestLoggingSender_Send** (the dev/staging default sender logs + always returns nil — the deterministic happy path before real transports are wired), covering the two pure 0% funcs under `make test`. Verify green: gofmt clean, `go vet -tags=integration`, `go test -tags=integration ./internal/service/ -run TestNotification` (4 tests pass, ~4.8s incl. container), default `go test ./internal/service/` green, `go build ./...` + `-tags=prod`, `make audit` ALL PASSED (CPM80=186µs / CPM200=484µs within gate; backup-db-test 6/6). **Next coverage candidates:** the `fmt.Errorf(…: %w)` store-error wrap legs across services, the worker-job handlers' live-DB paths (`internal/worker`), and `internal/api` handler-level integration.

- **2026-06-02** [`ae368ba` → `origin/main`] **DB-backed service-layer integration tests for the canonical `schedule.go` CPM recalc — `RecalculateSchedule` went 0% → 86.8% and `GetGantt` 0% → 85.7% against a real Postgres, exercising the full physics round-trip + transactional audit write + River cascade-enqueue (test-only, zero production change).** Closes the last major 0%-integration mutation service — the ARCHITECTURE.md flagship: load tasks/deps → `physics.ForwardPass`+`BackwardPass` → persist `early/late/float/is_critical` → audit → enqueue `DelayCascade` River job, all in one tx. New **`internal/service/schedule_integration_test.go`** (`//go:build integration`) wires a real `ScheduleService` via a `newScheduleService` helper that does two things beyond the usual org+project seed: (a) applies **River's own migrations** through `rivermigrate` (the `river_job` tables are NOT in `migrations/*.up.sql` — they're applied separately by `cmd/migrate`), and (b) builds an **insert-only River client** (`river.NewClient(driver, &river.Config{})`, no Workers/Queues) so the in-tx `InsertTx` of the cascade job has a real table to land in. A **real `NewAuditService` recorder** makes the `schedule.recalculated` write hit `audit_log`; `project_start_date` is pinned for a deterministic ForwardPass root; `seedSchedTask`/`seedSchedDep` build the DAG; `scheduleAuditCount`/`delayCascadeJobCount` assert directly against `audit_log`/`river_job`. Coverage: (1) **Recalculate_PersistsComputesAndEnqueues** — a 3-task FS chain recalcs: result returned (3 tasks, non-empty critical path, non-zero `ProjectEnd`, positive compute time), exactly **one** `schedule.recalculated` audit row, exactly **one** `delay_cascade` `river_job` row (proving the enqueue is committed in the same tx, not phantom), and the persisted `is_critical` flags round-trip through `GetGantt` matching the computed critical path exactly. (2) **Recalculate_CrossTenantHidden** — a foreign org recalcing another org's project → `ErrNotFound`, **zero** audit rows + **zero** enqueued jobs. (3) **Recalculate_NoTasks** — a task-less project fails the empty-graph guard, **zero** audit rows. (4) **GetGantt_NeverComputedAndCrossTenant** — a project whose tasks exist but were never recalced returns a non-nil empty critical path + zero `ProjectEnd` (the frontend's "run /recalculate" cue); foreign-org Gantt read → `ErrNotFound`. (5) **ListAndUpdateTask** — the DB-backed task list (status filter) + partial update (percent/status round-trip), both honoring the org guard (cross-tenant → `ErrNotFound`). (Note: the engine deterministically marks 2 of the 3 chain tasks critical under working-calendar float rounding — the test asserts persisted == computed rather than hardcoding a count, since CPM correctness is owned by the `internal/physics` golden-master tests.) Verify green: gofmt clean, `go vet -tags=integration`, `go test -tags=integration ./internal/service/ -run TestScheduleService` (5 tests pass, ~6s incl. container + River migrations), `go build ./...` + `-tags=prod`, `make audit` ALL PASSED (CPM80=210µs / CPM200=470µs within gate; backup-db-test 6/6). **Next coverage candidates:** the service layer's 0%-integration mutation surface is now closed; remaining lifts are the `fmt.Errorf(…: %w)` store-error wrap legs across services, the worker-job handlers (`internal/worker`), and `internal/api` handler-level integration.

- **2026-06-02** [`390b537` → `origin/main`] **DB-backed service-layer integration tests for the `PipelineService` prospect/estimate/permit surface — the lifecycle entrypoints went from 0% to 73–100% per-func against a real Postgres, with stage-transition + audit writes exercised transactionally (test-only, zero production change).** Continued the service-integration pattern (`05e5bc2` Project / `22bd8ba` Fleet / `df19b12` Procurement) on the largest mutation service — `PipelineService` is pool + `PipelineStore` + `riverClient` + audit. New **`internal/service/pipeline_integration_test.go`** (`//go:build integration`) wires a real `PipelineService` via a `newPipelineService` helper with a **real `NewAuditService` recorder** and a **nil river client** — every entrypoint except the PERMIT_ISSUED Kanban→CPM `transitionToPermitIssued` hop is river-independent, and that hop's nil-river `ErrNotImplemented` guard is already covered by the default-tag `pipeline_test.go` — plus a seeded org and `pipelineAuditCount`/`seedProspect` helpers. Coverage: (1) **ProspectCRUD_Lifecycle** — create defaults to **`LEAD`**/probability **10** → `GetProspectWithDetails` round-trip (empty estimates/permits) → `UpdateProspect` (rename+notes) → `ListProspects` (Total 1) → cross-tenant `GetProspectWithDetails` → `ErrNotFound`. (2) **ProspectCRUD_Validation** — blank name / blank client / negative GSF on create, negative GSF on update, bad stage filter on list → `ErrInvalidInput`. (3) **AdvanceAndLose** — walk the full stage chain LEAD→QUALIFIED→ESTIMATE_SENT→VERBAL_COMMITMENT→PERMIT_APPLIED (probability rising, one `pipeline.stage_transitioned` audit row per hop); a backward hop → `ErrInvalidTransition`; cross-tenant advance → `ErrNotFound`; `LoseProspect` (no reason → `ErrInvalidInput`; valid → **LOST**); advancing from LOST → `ErrTerminalStage`. (4) **Estimates** — `CreateEstimate` (2 line items, total computed) → `UpdateEstimateStatus` draft→sent; validation (bad currency / no items / margin>100 / bad status); cross-tenant → `ErrNotFound`. (5) **Permits** — `CreatePermit` (default `not_submitted`) → `UpdatePermit` →submitted; validation (no type / no jurisdiction / bad currency / bad status); cross-tenant → `ErrNotFound`. (6) **Analytics** — `GetPipelineAnalytics` succeeds on an org with prospects. Per-func lift: ListProspects 90%, GetProspectWithDetails 87.5%, CreateProspect 92.9%, UpdateProspect 90%, AdvanceProspect 86.4%, CreateEstimate 85.7%, UpdateEstimateStatus 73.7%, CreatePermit 90%, UpdatePermit 71.4%, GetPipelineAnalytics 100%, LoseProspect 77.8% (only the river-gated `transitionToPermitIssued` stays 0%). Verify green: gofmt clean, `go vet -tags=integration`, `go test -tags=integration ./internal/service/ -run TestPipelineService` (6 tests pass, ~7s incl. container spin-up), `go build ./...` + `-tags=prod`, `make audit` ALL PASSED (CPM80=183µs / CPM200=432µs within gate; backup-db-test 6/6). **Next coverage candidate:** the canonical `schedule.go` recalc — the last major 0%-integration mutation service (needs tasks+deps seeded → physics forward/backward pass → cascade-enqueue).

- **2026-06-02** [`df19b12` → `origin/main`] **DB-backed service-layer integration tests for the `ProcurementService` surface — all seven entrypoints went from 0% to 79–96% per-func against a real Postgres, including the AI-recommend persistence path (fake recommender) and the feed-card vendor-review flow, audit writes exercised transactionally (test-only, zero production change).** The richest mutation service so far: pool + `ProcurementStore` + AI recommender + `FeedCardsStore` + audit. New **`internal/service/procurement_integration_test.go`** (`//go:build integration`) wires a real `ProcurementService` via a `newProcurementService` helper with a **real `NewAuditService` recorder**, a **real `store.NewFeedCardsStore()`** (so `RequestVendorReview` actually persists a card), and an in-test **`fakeProcurementRecommender`** returning a canned ranked list (so the `RecommendVendors` persistence + audit path runs with no HTTP server / no Anthropic key), plus a seeded org+project and `procAuditCount`/`seedItem` helpers (reuses `ptrString` from `procurement_test.go`). Coverage: (1) **CreateItem_PersistsAndAudits** — name/wbs **trimmed**, default **`OK`** status, cost/currency persisted, one `procurement.item.created` row, `ListProcurement` round-trip. (2) **CreateItem_CrossTenantHidden** — create + list from a foreign org → `ErrNotFound`, **zero** audit rows. (3) **UpdateItem_TransitionsAndAudits** — OK→**ORDERED** (with the required PO number) + one `procurement.item.updated` row; non-existent item id → `ErrProcurementItemNotFound`; foreign-org update → `ErrNotFound`. (4) **RecomputeStatuses_FlipsThenIdempotent** — a long-lead item (must_order ~30d past) flips OK→**CRITICAL** on the first sweep (1 change), re-run is a no-op (0) — uses a far-past horizon so it's deterministic regardless of wall-clock date. (5) **RecommendVendors_PersistsAndAudits** — the 2-rec canned list is persisted (each row sharing the batch **RunID**), the item budget/currency context is threaded into the AI request, **one batch** `procurement.recommendations.created` audit row (not per-rec); cross-tenant item → `ErrProcurementItemNotFound`. (6) **RequestVendorReview_CreatesCardAndAudits** — produces a `vendor_review_requested` feed card (verified by a direct `feed_cards` read) + one `procurement.vendor_review.requested` row; cross-tenant item → `ErrProcurementItemNotFound`. (The pure helpers `formatCents`/`vendorReviewBody`/`confidenceToPct` + the validation/no-AI/no-feedstore sentinels were already covered by the default-tag `procurement_test.go`.) Verify green: gofmt clean, `go vet -tags=integration`, `go test -tags=integration ./internal/service/ -run TestProcurementService` (6 tests pass, ~7s incl. container spin-up), `go build ./...` + `-tags=prod`, `make audit` ALL PASSED. **Next coverage candidates:** `pipeline.go` (prospect/estimate/permit lifecycle + River enqueue — needs a `river.Client` or a nil-river path) and the canonical `schedule.go` recalc (needs tasks+deps seeded → physics → cascade-enqueue).

- **2026-06-02** [`22bd8ba` → `origin/main`] **DB-backed service-layer integration tests for the `FleetService` asset/allocation surface — the four entrypoints went from 0% to 87–96% per-func against a real Postgres, including the GiST exclusion-conflict path, audit writes exercised transactionally (test-only, zero production change).** Continued the service-integration pattern (`05e5bc2` ProjectService / `7455584` BudgetService) on the next clean mutation service — `FleetService` is pool + `FleetStore` + audit. New **`internal/service/fleet_integration_test.go`** (`//go:build integration`) wires a real `FleetService` via a `newFleetService` helper with a **real `NewAuditService` recorder** so the in-tx `fleet.asset.created`/`fleet.asset.allocated` writes hit `audit_log` (verified by a `fleetAuditCount` helper), plus a seeded org+project. Coverage: (1) **CreateAsset_PersistsAndAudits** — name/type **trimmed**, serial trimmed, default **`available`** status, one `fleet.asset.created` audit row, `ListAssets` round-trip. (2) **CreateAsset_BlankSerialNormalizes** — whitespace-only serial → NULL (not blank). (3) **CreateAsset_Validation** — nil org / blank name / blank type → `ErrInvalidInput`, **zero** audit rows. (4) **AllocateAsset_BooksAndConflicts** — first booking succeeds (one `fleet.asset.allocated` row); an **overlapping range trips the GiST exclusion constraint** → `ErrAllocationConflict` and the rolled-back tx leaves **no second audit row**; a non-overlapping range books cleanly (row count → 2). (5) **AllocateAsset_CrossTenantHidden** — a foreign org's vantage → `ErrFleetAssetNotFound` (asset guard runs first); own asset + a foreign project → `ErrNotFound`; **zero** audit rows. (6) **Validation** — the allocate pre-tx gates (nil asset/project, zero dates, end≤start → `ErrInvalidInput`) + the list gates (nil org, unknown status filter). Verify green: gofmt clean, `go vet -tags=integration`, `go test -tags=integration ./internal/service/ -run TestFleetService` (6 tests pass, ~7.4s incl. container spin-up), `go build ./...` + `-tags=prod`, `make audit` ALL PASSED (backup-db-test 6/6). **Next coverage candidates:** the remaining 0%-integration mutation services — `procurement.go`, `pipeline.go` (prospect/estimate/permit lifecycle + River enqueue), and the canonical `schedule.go` recalc (needs tasks+deps seeded → physics).

- **2026-06-02** [`05e5bc2` → `origin/main`] **DB-backed service-layer integration tests for the `ProjectService` CRUD surface — the four mutation/read entrypoints went from 35–43% to 82–92% per-func against a real Postgres, audit writes exercised transactionally (test-only, zero production change).** Extended the service-integration pattern (established by `7455584` for `BudgetService`) to the cleanest mutation service — `ProjectService` is just pool + `ProjectStore` + audit (no River/recommender). New **`internal/service/projects_integration_test.go`** (`//go:build integration`) wires a real `ProjectService` to a fresh migrated pool via a `newProjectService` helper with a **real `NewAuditService(store.NewAuditStore(), …)` recorder** (not the no-op) so the in-tx `project.created`/`project.updated` writes actually hit `audit_log`, plus a seeded org (`testdb.SeedOrg`) and a `projectAuditCount` helper querying `audit_log` directly. (Reuses the `strptr`/`intptr` helpers already in `projects_test.go` — no redeclare.) Coverage: (1) **CreateGetUpdate_Lifecycle** — create (name **trimmed** `"  Birch Lane New Build  "`→`"Birch Lane New Build"`; blank address **normalized to empty**; GSF 3200; **default `active` status**; exactly one `project.created` audit row) → `GetProject` round-trip scoped to org → partial `UpdateProject` (name+status→`completed`; one `project.updated` audit row). (2) **CrossTenantIsolation** — a project created under org A is invisible to org B: both `GetProject` and `UpdateProject` return `ErrNotFound` (existence not leaked) and **zero** update audit rows for the foreign org. (3) **ListProjects_FilterAndPaginate** — 3 active + 1 archived: unfiltered len 4, `status=active` len 3, `per_page=2` caps the page. (4) **Validation** — the pre-tx gates across all three entrypoints (GSF 9000 outside `[1500,6000]` / blank name / nil org → `ErrInvalidInput` on create; bad `status` filter on list; empty patch + blank-name patch on update → `ErrInvalidInput` before any row is touched). Verify green: gofmt clean, `go vet -tags=integration`, `go test -tags=integration ./internal/service/ -run TestProjectService` (4 tests pass, ~4.8s incl. container spin-up), `go build ./...` + `-tags=prod`, `make audit` ALL PASSED (CPM80=141µs / CPM200=471µs within gate; backup-db-test 6/6). **Next coverage candidates:** the same pattern for the remaining 0%-integration mutation services — `pipeline.go` (prospect/estimate/permit lifecycle + River enqueue), `procurement.go`, `fleet.go`, and the canonical `schedule.go` recalc (needs tasks+deps seeded → physics).

- **2026-06-02** [`7455584` → `origin/main`] **First DB-backed service-layer integration tests for the financial mutation surface — `BudgetService` `CreateInvoice`/`UpdateInvoice` went from 0% to 93–95% against a real Postgres, with the audit write exercised transactionally (`internal/service` integration 42.3% → 46.7%, test-only, zero production change).** Took the "next coverage candidate" the prior entry named: cloned the store-test `testdb.NewPool` pattern UP into the service layer. New **`internal/service/budget_integration_test.go`** (`//go:build integration`) wires a real `BudgetService` to a fresh migrated pool via a `newBudgetService` helper — crucially with a **real `NewAuditService(store.NewAuditStore(), …)` recorder** (not the no-op) so the in-tx `s.audit.Record` write actually hits `audit_log`, covering the audit branch the unit tests skip — plus a seeded org+project (`testdb.SeedOrg`/`SeedProject`) and an `auditCount` helper that queries `audit_log` directly. Coverage: **CreateInvoice** — happy round-trip (DB-assigned id + default `pending` status + amount/currency persisted) **and exactly one `invoice.created` audit row written in the same tx** (proving the audit is transactional with the mutation); the three pre-tx input gates (bad currency / empty vendor / non-positive amount → `ErrInvalidInput`, **and zero audit rows** — rejected before any write); **cross-tenant** create against another org's project → `ErrNotFound` (existence not leaked). **UpdateInvoice** — pending→paid transition (returned model reflects new status + an `invoice.updated` audit row joins the created one); bad-status pre-tx gate → `ErrInvalidInput`; cross-tenant update → `ErrNotFound`. **Reads** — the optional-currency gate shared by `GetARAging`/`GetProjectFinancials`/`GetOrgFinancialsSummary` (supported code → empty result on an empty org; `EUR`/`GBP` → `ErrInvalidInput`) + `GetProjectBudgets` round-trip. **RunCorporateRollup** — idempotent quarter upsert (0 rows over an org with no budgets; re-run stays 0). `RunCorporateRollup` was the last 0% func in budget.go; now only the `fmt.Errorf(...: %w)` store-error wrap legs remain. Verify green: gofmt clean, `go vet -tags=integration`, `go test -tags=integration ./internal/service/ -run TestBudgetService` (6 tests pass, ~6s incl. container spin-up), default `go test ./internal/service/` still green, `go build ./...` + `-tags=prod`, `make audit` ALL PASSED (CPM80/200 within gate; backup-db-test 6/6). **Next coverage candidates:** the same pattern for the other 0%-integration mutation services — `pipeline.go` (prospect/estimate/permit lifecycle + River enqueue), `procurement.go`, `fleet.go`, `projects.go` (partial), and the canonical `schedule.go` recalc (needs tasks+deps seeded → physics).

- **2026-06-02** [`c2aafc0` → `origin/main`] **Partial-branch handler tests in `internal/api` — covered the named error/degraded legs that the happy-path suites skipped (`readinessHandler` 5%→95%, `CreateInvoice` 80%→95%, `UpdateInvoice` 65%→91%, `DailyBriefing` 80%→100%; `internal/api` 84.6% → 88.6%, test-only, zero production change).** Closed the three coverage candidates the prior entries flagged. (1) **`internal/api/readiness_test.go`** (new) — the readiness probe's **DB-down leg** (the path the load balancer polls to pull a degraded instance) was at 5%; covered it offline via a `deadPool` helper (`pgxpool.New` aimed at refused `127.0.0.1:1` — connects lazily so construction is cheap, the probe's own 2s deadline bounds the failed dial) asserting **503 + `application/json` + `{"status":"unhealthy","components":{"database":"unhealthy"}}`**, plus a **nil-logger** variant (probe wired before obs in some boot orders must not panic). The 200-OK leg still needs a live DB (integration). (2) **`internal/api/financials_test.go`** (extended) — `CreateInvoice` bad-`due_date` → 400 + invalid-projectID → 400; `UpdateInvoice` **happy path 200** (was only reached via the two error sentinels), bad-JSON → 400, bad-`paid_date` → 400, invalid-invoiceID → 400. (3) **`internal/api/agents_test.go`** (extended) — `DailyBriefing` **invalid-org-claim → 401** (unparseable `claims.OrgID`; asserts the service is NOT invoked), AI-upstream **5xx → 502** `UPSTREAM_ERROR`, and the **generic-error → 500** `INTERNAL_ERROR` default leg (asserts the raw error detail is NOT leaked into the body — pushed `writeServiceError` 87%→91%). All reuse the package `buildRequest`/`deadPool` + existing mocks; no new infra. Verify green: gofmt clean, `go vet`, `go test ./internal/api/`, `go build ./...` + `-tags=prod`, `make audit` ALL PASSED (CPM80=175µs / CPM200=482µs within gate; backup-db-test 6/6). **Remaining `internal/api` gaps** are the live-DB legs (`readinessHandler`/`NewRouter` 200 paths) + the many `pipeline.go`/`setup.go` handler branches (58–77%). **Next coverage candidate:** DB-backed service mutations behind the `integration` tag (clone the store-test `testdb.NewPool` pattern up into `service/*_integration_test.go`).

- **2026-06-02** [`7657328` → `origin/main`] **Store-layer integration tests for the two untested native-auth/vault stores — `users.go` + `integration_credentials.go` went from 0% to ~75–100% per-func (`internal/store` integration coverage now 62.8%; test-only, zero production change).** Audit of the store package found exactly two source files with **no** integration test — and both back the most security-critical surfaces added in the standalone pivot: `UserStore` (login / refresh-token rotation / password reset) and `IntegrationCredentialStore` (the encrypted BYOK vault). Added two `//go:build integration` files using the existing `testdb.NewPool` + `pgx.BeginTxFunc` + `testdb.SeedOrg` pattern (ephemeral migrated Postgres, fresh org per test for isolation). (1) **`internal/store/users_integration_test.go`** — `CreateUser` round-trip (locale defaults `en-US`; role/hash persisted; fresh `last_login_at` NULL) + **case-insensitive** lookups (`GetUserByEmail`/`GetUserByEmailGlobal`/`GetUserByID` all resolve mixed-case `Owner@Example.com` to the same row, original casing preserved); duplicate-email **unique-violation** on `users_org_email_uidx` (case-folded); `ErrNotFound` on all three lookup misses; `CountUsersInOrg` (0 → 1, backs first-owner bootstrap); `UpdatePasswordHash` + `TouchLastLogin` (persist + `ErrNotFound` on missing id); **refresh-token lifecycle** (`CreateRefreshToken`→`GetActiveRefreshToken` by hash; **expired** excluded by evaluating a future `now`; `RevokeRefreshToken` drops it from active; double-revoke → `ErrNotFound` race-detect); `RevokeAllRefreshTokensForUser` (3 active → 3 revoked, second sweep 0 — logout-everywhere); **password-reset-token lifecycle** (create→get by hash; expired excluded; redeem drops active; **replay** redeem → `ErrNotFound`). (2) **`internal/store/integration_credentials_integration_test.go`** — `UpsertActive` **rotation** (second upsert for the same `(org,provider)` inserts a NEW row AND deactivates the prior so the partial unique index `integration_credentials_active_uidx` never sees two active rows — proves the deactivate+insert run in one tx; exactly 1 active of 2 total); `GetActiveByProvider` **soft-fail** `ErrNotFound` when nothing active (the resolver's "AI unconfigured" path); `DeactivateByProvider` (1 row affected → then 0 on the no-op second call, which the service maps to `ErrNotFound`); `ListByOrg` **org isolation** + newest-first (org A's two providers never leak org B's row). The store never inspects the sealed bytes (cryptobox seals in the service), so opaque test ciphertext/nonce suffice. Remaining per-func gaps are the `fmt.Errorf(...: %w)` DB-error wrap legs (only reachable via fault injection). Verify green: gofmt clean, `go vet -tags=integration`, `go test -tags=integration ./internal/store/` (full suite 62.8%, ~88s incl. container spin-up), `go build ./...` + `-tags=prod`, `make audit` ALL PASSED. **Note:** NEXT_STEPS.md Tier-1 (audit-JSONB scrub, structured-log scrub, schedule+pipeline audit recording) is **stale — all three are already shipped** (`scrubAuditPayloads`, `scrubAttr`, and the `s.audit.Record` calls in schedule.go/pipeline.go); Tier-2/3 still reference the removed Brain. **Next coverage candidates:** the DB-backed service mutations (behind the integration tag) and the partial `internal/api` branches (`readinessHandler` DB-down, `CreateInvoice`/`DailyBriefing` error legs).

- **2026-06-02** [`1919dfc` → `origin/main`] **Pure-helper unit tests in `internal/service` — covered the dependency-free validation + error-mapping + formatting helpers that guard every mutation but sat at 0% under the default (no-DB) tag (`internal/service` 14.3% → 16.5%, test-only, zero production change).** The bulk of `internal/service` is exercised only behind the `integration` tag (real Postgres), but a ring of **pure functions** — input gates, store→service error mappers, and currency-safe formatters — run *before* any pool/store call and are unit-testable with no fixture. Added/extended four default-tag files: (1) **`internal/service/auth_test.go`** (new) — `validateEmail` (permissive structural gate: accepts `a@b`/tagged/sub-domain; rejects empty, no-`@`, empty local/domain, and embedded space/tab/newline → `ErrInvalidInput`), `validatePassword` (length floor exactly at 12: 11 rejects, 12 accepts), `mapAuthStoreError` (Postgres `23505` unique-violation → `ErrInvalidInput` carrying the constraint name so the handler returns 400/409 not 500; `store.ErrNotFound`→`ErrNotFound`; nil→nil; **non-23505 pg error passes through** so it still surfaces a 500 rather than being mis-downgraded). (2) **`internal/service/integrations_helpers_test.go`** (new) — `lastNRunes` (the non-secret last4 display hint: **rune-safe**, won't split a multi-byte tail/emoji; returns the whole string when shorter than n; empty-safe), `mapVaultStoreError` (same `23505`→`ErrInvalidInput` / `ErrNotFound` / passthrough shape as the auth mapper). (3) **`internal/service/budget_test.go`** (extended) — `validateOptionalCurrency` (empty allowed → column defaults USD; USD/CAD pass; `EUR`/garbage → `ErrInvalidInput`), `mapStoreError` (the package-shared base mapper: nil→nil, `store.ErrNotFound`→`ErrNotFound`, other→passthrough). (4) **`internal/service/procurement_test.go`** (extended) — `formatCents` (Composite-Currency-Pattern renderer, **never touches a float**: zero-pads sub-dollar `5`→`0.05`, two-digit cents remainder, sign on the whole value `-2500`→`-25.00`), `vendorReviewBody` (operator-facing feed summary embeds vendor + `formatCents` amount + currency + quoted item name). All construct `*pgconn.PgError{Code:"23505"}` directly — no DB. Verify green: gofmt clean, `go vet`, `go test` (all pass), integration-tag build still compiles (no symbol clash with the `*_integration_test.go` files), `go build -tags=prod ./...`, `make audit` ALL PASSED. **Next coverage candidates:** the DB-backed service mutations + `internal/store` (0.4% default; bulk behind the integration tag — would need a `testdb`-pool integration suite) and the partial `internal/api` branches (`readinessHandler` DB-down, `CreateInvoice`/`DailyBriefing` error legs).

- **2026-06-02** [`40074db` → `origin/main`] **Router-level RBAC smoke tests for the vault surface — closed the last two route-registration 0%-coverage funcs (`MountIntegrationRoutes`/`MountCapabilitiesRoutes` now 100%; `internal/api` 83.9% → 84.6%, test-only, zero production change).** The handler-level tests covered every handler but the two `Mount*` helpers (and the RBAC wiring they install) were only reachable through the real router. Extended **`internal/api/router_test.go`** (alongside the existing boot-panic regression guards) to build the **real `NewRouter`** with `DevAuthMode:"header"` + a `mockIntegrationsService`, then drive requests through the full middleware stack via a small `authedGet(target, role)` helper (the `X-Dev-Auth: sub,org,role` bypass lets a router test exercise any role without minting JWTs; `SetupService` left nil so `SetupGate` is skipped and operational routes stay reachable). Cases: **`GET /api/v1/capabilities` as `field_worker` → 200** (proves `MountCapabilitiesRoutes` is wired AND **auth-only, NOT admin-gated** — the contract the per-role UIs depend on); **`GET /api/v1/integrations` as `admin` → 200** (proves `MountIntegrationRoutes` + its `RequireMinRole(admin)` gate admits admin); **same as `superintendent` → 403** (one rung below admin — proves the gate *discriminates*, pairing with the admin-OK case to rule out allow-all/deny-all); **both paths with `IntegrationsService` nil → 404** (proves the conditional mount: vault disabled ⇒ neither surface mounts ⇒ frontend assume-on fallback). This is the first test to drive the production `NewRouter` route tree end-to-end (auth → SetupGate-skip → RBAC → handler), so it also guards the mount-order + middleware-group wiring beyond just the two helpers. Verify green: gofmt clean, `go vet`, `go test` (all pass), `go build -tags=prod ./...`, `make audit` ALL PASSED (CPM80=182µs / CPM200=468µs within gate; backup-db-test 6/6). **`internal/api` now has no 0%-coverage funcs** — remaining gaps are partial branches (`NewRouter` agents-wiring, `readinessHandler` DB-down path, `CreateInvoice`/`DailyBriefing` error legs). **Next coverage candidates (other packages):** `internal/service` (14.3% default-tag — bulk behind the integration tag) and `internal/store` (0.4%).

- **2026-06-02** [`60d7af9` → `origin/main`] **Procurement CRUD + financials org-read HTTP handler tests — closed the LAST handler-level 0%-coverage gaps in `internal/api` (test-only, zero production change; `internal/api` 77.7% → 83.9%).** Two files extended: (1) **`internal/api/procurement_test.go`** — the `RequestVendorReview` handler was already covered, but `List`/`Create`/`Update` + the `parseOptionalRFC3339` helper sat at 0%. Extended the existing `mockProcurementService` (was stubbing List/Create/Update with arg-dropping no-ops) to **record** the forwarded list-(proj,org,status) / `CreateProcurementItemInput` / `UpdateProcurementItemInput` args, and added: **List** (200 + `?status=` CSV split forwarded / bad projectID → 400 / service-error → 500); **Create** (201 + the full `CreateProcurementItemInput` incl. `estimated_cost_cents`/`currency_code` + `need_by_date` parsed via `parseOptionalDate` (`YYYY-MM-DD`) + `claims.Sub` forwarded / bad projectID → 400 / bad-JSON → 400 / unparseable `need_by_date` → 400 / service `ErrInvalidInput` → 400); **Update** (200 + `ItemID`/`ProjectID` + `status`/`po_number` pointers + `ordered_at` parsed via the RFC3339-only `parseOptionalRFC3339` / bad itemID → 400 / bad-JSON → 400 / **date-only `ordered_at` → 400** (RFC3339-only, distinct from Create's lenient parser) / `ErrProcurementItemNotFound` → 404); the full `writeServiceError` mapping table (vendor-review-unavailable → 503, item-not-found → 404, project `ErrNotFound` → 404, validation → 400, default → 500); and a direct **`parseOptionalRFC3339`** test (nil/empty → nil,nil; date-only → error; RFC3339 → parsed). (2) **`internal/api/financials_test.go`** — `Summary` was covered but the sibling org-reads `ARAging` + `ProjectFinancials` (same `requireOrgIDFromURL` + currency + `writeServiceError` shape) sat at 0%: added `ARAging` (200 + org/currency forwarded + `{snapshots}` envelope / org-mismatch → 403 / service-error → 500) and `ProjectFinancials` (200 + org/currency forwarded + `{projects}` envelope / service `ErrInvalidInput` → 400). All handlers in `internal/api` are now covered — **the only remaining 0% funcs are the two route-registration helpers `MountIntegrationRoutes`/`MountCapabilitiesRoutes`** (exercised at the router level, not unit-testable in isolation). Verify green: gofmt clean, `go vet`, `go test` (all cases pass), `go build -tags=prod ./...`, `make audit` ALL PASSED (CPM80/200 within gate; backup-db-test 6/6). **Next coverage candidates (other packages):** `internal/service` (14.3% default-tag — bulk behind the integration tag) and `internal/store` (0.4%); or a router-level smoke test to cover the `Mount*` helpers + RBAC wiring.

- **2026-06-02** [`3bc9c58` → `origin/main`] **Integrations credential-CRUD HTTP handler tests — closed the remaining 0%-coverage gap on the encrypted-vault admin surface (test-only, zero production change; `internal/api` 74.2% → 77.7%).** `internal/api/integrations.go` already had `Capabilities` covered, but the three vault-CRUD handlers — `List` (GET `/integrations`), `Set` (PUT `/integrations/{provider}`), `Delete` (DELETE `/integrations/{provider}`) — plus the shared `writeIntegrationError` mapper sat at 0%. Extended the existing `mockIntegrationsService` to **record** the forwarded `SetCredentialInput` / list-org / delete-(org,provider,sub) args (was previously arg-dropping) and added handler tests to **`internal/api/integrations_test.go`**: **List** (200 + the DTO maps `provider`/`last4`/`is_active` AND — security-critical — the JSON body **never** leaks the model's `ciphertext`/`nonce`/`key_version` secret bytes; empty list serializes as `[]` not `null`; service-error → 500); **Set** (200 + the mixed-case/padded `{provider}` URL param is **lowercased+trimmed** before reaching the service, `key`/`label`/`OrgID`/`claims.Sub` all forwarded, returned DTO shape / empty-provider → 400 / bad-JSON → 400 / missing-`key` → 400 VALIDATION_ERROR / service `ErrInvalidInput` → 400); **Delete** (204 with empty body + provider lowercased/trimmed + org/sub forwarded / empty-provider → 400 / `ErrNotFound` → 404); and a direct **`writeIntegrationError`** mapping table (not-found → 404, validation → 400, default → 500-no-leak). Reused the package `buildRequest` (claims + chi params) + the shared `errInternal`/`wrapInvalid`/`decodeErrCode`/`contains` helpers (no new infra). Verify green: gofmt clean, `go vet`, `go test` (all cases pass), `go build -tags=prod ./...`, `make audit` ALL PASSED (CPM80/200 within gate; backup-db-test 6/6). **Next `internal/api` candidates:** `procurement.go` write paths, `agents.go`, and remaining `pipeline.go`/`projects.go`/`schedule.go` handler branches.

- **2026-06-02** [`e50ba96` → `origin/main`] **Feed + Field (mobile-sync) HTTP handler tests — closed the 0%-coverage gap on the notification-feed and Flutter field-sync surfaces (test-only, zero production change; `internal/api` 65.8% → 74.2%).** `internal/api/feed.go` (`FeedHandler`: `List`/`Action`/`Dismiss` + `writeServiceError` + the `splitCSVParam` helper) and `internal/api/field.go` (`FieldHandler`: `Sync`/`ReportProgress`/`Checkin`/`DailyLog` + `writeServiceError` + the `claimsAndOrg` helper) both had **no** test. New **`internal/api/feed_test.go`** + **`internal/api/field_test.go`** drive every handler through `fakeFeedService` / `fakeFieldService` (record forwarded args), reusing the package `buildRequest`; both surfaces are **caller-scoped (org from claims, never the URL)** so the small `feedReq`/`fieldReq` wrappers omit the `orgID` URL param. Coverage — **feed**: `List` (CSV `?status=`/`?priority=` split + `?page=`/`?per_page=` all reach the service opts / invalid-org-claim → 401 / service-error → 500); `Action` (200 + the decoded `action_type` + opaque `payload` JSON + `cardID` + `claims.Sub` reach the service / bad cardID → 400 / bad-JSON → 400 / `ErrFeedCardNotFound` → 404); `Dismiss` (200 + card/sub forwarded / bad cardID → 400 / service-error → 500); the `writeServiceError` mapping table (card-not-found → 404, validation → 400, default → 500); and a direct **`splitCSVParam`** test (missing → nil, whitespace-trimmed + empty-segments-dropped). **field**: `Sync` (200 + `?since=` RFC3339 parsed onto opts / no-since → zero-time / bad since → 400 / invalid-org-claim → 401 / service-error → 500); `ReportProgress` (201 + `task_id`/`percent_complete`/`idempotency_key`/`claims.Sub` forwarded / bad-JSON → 400 / `ErrIdempotencyConflict` → 409 CONFLICT); `Checkin` (201 + `project_id`/opaque `crew_members` JSON/`idempotency_key` forwarded / bad-JSON → 400); `DailyLog` (201 + `project_id`/`work_summary`/`log_date` (`YYYY-MM-DD`)/`idempotency_key` forwarded / bad-JSON → 400 / unparseable `log_date` → 400 VALIDATION_ERROR); the `writeServiceError` mapping table (idempotency → 409, not-found → 404, validation → 400, default → 500). Reused the existing package fakes-and-`buildRequest` pattern + the shared `errInternal`/`wrapInvalid`/`decodeErrCode` helpers (no new infra). Verify green: gofmt clean, `go vet`, `go test` (all cases pass), `go build -tags=prod ./...`, `make audit` ALL PASSED (CPM80=187µs / CPM200=356µs within gate; backup-db-test 6/6). **Next `internal/api` 0%-handler candidates:** `procurement.go` write paths (the integrations credential-CRUD handlers `List`/`Set`/`Delete`, and any remaining `agents.go`/`pipeline.go` branches).

- **2026-06-02** [`a34e575` → `origin/main`] **Fleet + HR HTTP handler tests — closed the 0%-coverage gap on the equipment/employee surface (test-only, zero production change; `internal/api` 59.5% → 65.8%).** `internal/api/fleet.go` (two handlers in one file — `FleetHandler` + `HRHandler`) had **no** test: `ListAssets`, `CreateAsset`, `AllocateAsset`, `ListEmployees`, `ListCertifications`, both `writeServiceError` mappers, and the `parseRequiredDate` helper all sat at 0%. New **`internal/api/fleet_test.go`** drives every handler through `fakeFleetService` / `fakeHRService` (record forwarded args), reusing the package `buildRequest` (installs JWT claims + chi URL params) via a small `fleetReq` wrapper that defaults the URL `orgID` to the caller's claims org so `requireOrgIDFromURL` passes. Coverage: **ListAssets** (CSV `?status=` split reaches the service / **org-mismatch → 403 FORBIDDEN** when the URL org ≠ claims org / service-error → 500); **CreateAsset** (201 + the decoded `CreateAssetInput` incl. `serial_number` pointer + `claims.Sub` reach the service / bad-JSON 400 / `ErrInvalidInput` → 400); **AllocateAsset** (201 with **mixed date formats** — `start_date` as `YYYY-MM-DD`, `end_date` as RFC3339, both parsed and forwarded / unparseable date → 400 / `ErrAllocationConflict` → 409 CONFLICT); the two `writeServiceError` mapping tables (fleet: asset-not-found + project `ErrNotFound` → 404, conflict → 409, validation → 400, default → 500; HR: employee-not-found → 404, validation, default); **ListEmployees** (200 + org / service-error 500); **ListCertifications** (200 + `employeeID` forwarded / malformed `employeeID` UUID → 400); and a direct **`parseRequiredDate`** test (empty → error, garbage → error, `YYYY-MM-DD` and RFC3339 both parse to the right instant). Verify green: gofmt clean, `go vet`, `go test` (all cases pass), `go build -tags=prod ./...`, `make audit` ALL PASSED (CPM80/200 within gate; backup-db-test 6/6). **Next `internal/api` 0%-handler candidates:** `feed.go`, `field.go`, `procurement.go` (List/Create/Update), and the integrations credential-CRUD handlers (`List`/`Set`/`Delete`).

- **2026-06-02** [`ba1bb5b` → `origin/main`] **Native-auth HTTP handler tests — closed the 0%-coverage gap on the unauthenticated `/api/v1/auth/*` surface (test-only, zero production change; `internal/api` 54.8% → 59.5%).** The entire native identity surface (`internal/api/auth.go` — the handlers that mint the credentials the rest of the API requires) had **no** test: `ClaimFirstOwner`, `Login`, `Refresh`, `Logout`, `RequestPasswordReset`, `ResetPassword`, plus `newTokenPairResponse` and the `writeAuthError` sentinel→HTTP mapper all sat at 0%. New **`internal/api/auth_test.go`** drives every handler through a `fakeAuthService` (records the args each handler forwards so the test proves the decoded body reaches the service verbatim; matches the existing `mock*Service` pattern): **happy paths** assert the right status (claim 201 / login + refresh 200 / logout + reset-confirm 204 / reset-request 202), that the `{data:{access_token, token_type:"Bearer", expires_in, refresh_token, user}}` envelope is shaped correctly, and that 204 responses carry an empty body; **bad-JSON** on all six → 400 VALIDATION_ERROR; one representative sentinel per handler (`ErrFirstOwnerExists`→409, `ErrInvalidBootstrapToken`→401, `ErrInvalidCredentials`→401, `ErrInvalidRefreshToken`→401, `ErrInvalidResetToken`→400, a non-sentinel Logout error→500 INTERNAL_ERROR with the raw error NOT leaked into the body). A dedicated **`TestWriteAuthError_Mapping`** table hits every branch of the mapper directly — including the `%w`-wrapped `ErrInvalidInput`→400 and the default→500-no-leak path (asserts the secret internal detail never appears in the response). Verify green: gofmt clean, `go vet`, `go test` (all 22 cases pass), `go build -tags=prod ./...`, `make audit` ALL PASSED (CPM80/200 within gate; backup-db-test 6/6). **Next `internal/api` 0%-handler candidates:** `feed.go`, `field.go`, `fleet.go`, `procurement.go`, and the integrations credential-CRUD handlers (`List`/`Set`/`Delete` — the `Capabilities` handler is already covered).

- **2026-06-02** [`6dfcd77` → `origin/main`] **Auth + RBAC middleware tests — closed the last 0%-coverage gap on the security-critical request gate (test-only, zero production change; `internal/api/middleware` 59.3% → 98.9% default-tag, 95.4% `-tags=prod`).** The middleware that gates EVERY authenticated request — JWT/header auth + role enforcement — had several core functions at 0%: `Auth`, `MustClaimsFromContext`, `withRequestCorrelation` (auth.go) and all three RBAC guards `RequireRole`/`RequireMinRole`/`RequireWriteRole` (rbac.go). New **`internal/api/middleware/rbac_test.go`** (DB-free, table-driven over a `nextRecorder` terminal handler + `reqWithRole` helper that installs claims via the exported `ContextWithClaims`): `RequireRole` (allowed-set passes / disallowed → 403 / no-claims → 401), `RequireMinRole` (exact level passes / higher level passes / lower → 403 / no-claims → 401 / unknown USER role → 403 / unknown MIN role blocks even owner — the `level 99` fail-closed default), `RequireWriteRole` (GET/HEAD/OPTIONS pass with NO claims since reads are unguarded here / POST owner + PUT admin pass / POST field_worker + DELETE superintendent → 403 / POST no-claims → 401). New **`internal/api/middleware/auth_handler_test.go`** (untagged — compiles + runs under prod too) exercises the real RS256 path with a per-test `rsa.GenerateKey`-backed `auth.TokenIssuer`/`Verifier` pair: a valid Bearer token stamps `Sub/OrgID/Role/PlanTier` onto the next handler's context; the rejection matrix asserts 401 + the exact error-body string + next-NOT-called for missing header / no-`Bearer` prefix / single-token / garbage-token / missing-required-claims (empty-role token passes signature but trips the presence gate); a forged-expired token (mint against a `WithClock` 1h-past + `WithAccessTTL` 1m so the real-clock verifier sees `exp` behind now) hits the distinct `jwt.ErrExpired` → "token expired" branch; a `chimw.RequestID`-wrapped request proves `withRequestCorrelation` propagates the chi request id onto the context under the obs key (and the unwrapped case leaves it empty); plus `MustClaimsFromContext` returns-when-present / panics-when-absent (via `recover`). The dev-only header-mode path lives in **`internal/api/middleware/auth_handler_dev_test.go`** (`//go:build !prod`, since `claimsFromDevHeader` is compiled out of prod — D8): valid `X-Dev-Auth` → claims passed through; malformed (two fields) → 401 "X-Dev-Auth invalid". Verify green: gofmt clean, `go vet`, `go test` default (98.9%) + `-tags=prod` (95.4% — the lower number is exactly the header-mode lines that don't exist in prod), `go build ./...` + `-tags=prod`, `make audit` ALL PASSED (CPM80=192µs CPM200=485µs within gate; backup-db-test 6/6). Remaining uncovered = the `IsProdBuild` build-mode reporter + a couple defensive nil-branches.

- **2026-06-02** [`9c8efcb` → `origin/main`] **River worker package tests — closed a whole-package 0%-coverage gap (test-only, zero production change; `internal/worker` 0% → 69.2% default-tag, 88.5% with integration).** The River job daemon (`jobs.go` + `registry.go`) had **no** tests at all despite owning two correctness-critical contracts: the persisted job-**kind** strings and the notification retry schedule. New **`internal/worker/jobs_test.go`** (DB-free): **`TestJobArgsKind`** pins all 10 `Kind()` strings (`daily_briefing`, `procurement_check`, `hydrate_project`, `corporate_rollup`, `certification_alerts`, `maintenance_reminders`, `field_notification_retry`, `delay_cascade`, `pipeline_analytics`, `permit_issued_transition`) + asserts no duplicates — a rename would orphan every already-queued `river_job` row of that kind, so this is a wire-protocol contract; `FieldNotificationRetryArgs.InsertOpts()` MaxAttempts=6; **`NextRetry`** custom backoff (30s/60s/2m/5m/30m/60m by attempt, clamps to the last delay past the schedule — asserted via `got.Sub(before)` bands to stay non-flaky); the three nil-dependency constructor **panics** (`NewProcurementCheckWorker`/`NewCorporateRollupWorker`/`NewFieldNotificationRetryWorker` — fail-fast at wiring, not at first tick) + happy delegation through fakes (`ProcurementCheckWorker`/`CorporateRollupWorker` wrap their service error with `errors.Is`; `FieldNotificationRetryWorker` passes `job.Attempt` + args straight to the deliverer and propagates its error); and the 7 placeholder workers' `Work` return nil (a non-nil there would make River retry a no-op forever). Plus **`internal/worker/registry_integration_test.go`** (`//go:build integration`, `testdb.NewPool`): `NewRegistry` builds the River client + registers all 10 workers cleanly (a duplicate kind / bad config would fail here) and panics fast on empty `Dependencies{}`. Verify green: gofmt clean, `go vet` (default + `-tags=integration`), `go build ./...` + `-tags=prod`, worker integration test passes against Testcontainers Postgres (2.6s), `make audit` ALL PASSED (CPM80/200 within gate; backup-db-test 6/6). Remaining worker uncovered = the inline periodic-job closures + `Start` (a blocking `river.Client.Start`), both genuinely runtime-only.

- **2026-06-02** [`097192f` → `origin/main`] **Scoping engine + equipment validator tests — closed the last two 0%-coverage files in the physics core (test-only, zero production change; raises `internal/physics` 56.9% → 88.7% default-tag, higher with integration).** Two new files. **(1) `internal/physics/scoping_test.go`** (DB-free, deterministic) covers the scope-adaptive WBS generator `ApplyScope` + every rule helper: **foundation** (case-insensitive `slab` removes waterproofing/drain 8.7/8.8; `basement` adds the 8.12→8.13→8.14 chain and wires the FS deps; unknown type is a no-op), **dangling-dep filtering** (a dep into a removed task is dropped from the output), **stories** (=1 removes 9.2 + cuts 9.1 30%→7.0; ≥3 adds 9.8 + bumps 9.1/9.2/9.3 30%→13.0; =2 is neutral), **size** (gsf>4000 adds 7.5 pred 7.4; exactly 4000 is a no-op — boundary), **topography** (`hillside` adds 7.6 + extends non-inspection 8.* 40%→14.0 while the 8.2 inspection is untouched; flat is neutral), **in-progress** (exact code → 0.5-day marker; `8.x` wildcard collapses every 8.* to 0.5 while 9.* is untouched; no-match → no change), plus `CompletedTaskCodes` (wildcard expansion + unknown-code rejection), `IsTaskCompleted` (exact / wildcard-prefix / empty), and `newDep` (FS, lag 0). Reuses the package-shared `almostEqual` from `dhsm_test.go` for the float-multiplier assertions. **(2) `internal/physics/equipment_validator_test.go`** (DB-free) covers the prefix/requirement gates that short-circuit **before** the COUNT query — non-7.x and unmapped-7.x both return nil with a **nil pool** (proving no DB touch), `ValidateProjectEquipment` skips no-equipment tasks, and the `SitePrepEquipmentRequirements` map contents — **plus `internal/physics/equipment_validator_integration_test.go`** (`//go:build integration`, `testdb.NewPool`) exercising the REAL `fleet_assets`+`equipment_allocations` COUNT query (migration 003): unallocated grader (7.4) → error, allocate a grader covering the window → nil, a different unallocated type (7.3 compactor) → still error; and the batch path collects exactly one warning (7.4/grader missing) while the covered excavator task (7.1) and the non-site-prep task (9.1) stay clean. Verify green: gofmt clean, `go vet` (default + `-tags=integration`), `go build ./...` + `-tags=prod`, both integration tests pass against Testcontainers Postgres (2.5s), `make audit` ALL PASSED (CPM80/200 within gate; backup-db-test 6/6). With this, every `internal/physics` source file now has direct test coverage.
- **2026-06-02** [`bf684dd` → `origin/main`] **DHSM duration-model unit tests — closed a 0%-coverage gap in the deterministic physics core (test-only, zero production change; raises `internal/physics` 44.1% → 56.9%).** `internal/physics/dhsm.go` (the Dynamic Hierarchical Scaling Model that feeds CPM) had **no** direct test despite "determinism is non-negotiable" — every exported function plus the package-internal helpers sat at 0%. New `internal/physics/dhsm_test.go` (DB-free, deterministic, plain `testing` to match `swim_test.go`): **`CalculateSAF`** (gsf≤0 → exact 1.0; standard 2000sqft → exact 1.0; 2^0.35 / 0.5^0.35 curve via tolerance; explicit-cfg honored) + a monotonicity test across the 1500–6000 GSF band; **`CalculateSAFScaled`** (×1000 fixed-point rounding, 0/2000 → 1000); **`getContextVariable`** (all three keys + unknown/empty → 0); **`applyMultiplierFormula`** (linear / scaled-with-2000-baseline-neutral / step / unknown+empty → linear fallback); **`DurationToDays`** + **`DaysToDuration`** (30-min-grid rounding: 14.4min → down to 0, 28.8min → up to 30min) + a grid-aligned round-trip; **`CalculateTaskDuration`** (float, deprecated) — standard-size no-op, inspection ignores SAF regardless of GSF, half-day `ceil` rounding, linear-multiplier application, multiplier skipped for non-matching code, multiplier skipped on zero context value, weather-sensitive WBS (`8.0`) applies the ×1.15 rain multiplier; **`CalculateTaskDurationV2`** (the deterministic int64-nanos path) — standard-size 48h, inspection 24h, 2.1d → rounds up to 52h, and a property check that output is always a `HalfDay` multiple across several fractional bases; **`CalculateBatchDurations`/`V2`** (length, per-task `WBSCode`/`BaseDuration` fields, and `CalculatedDuration` equals the single-call result). Verify green: gofmt clean, `go vet`, `go build ./...` + `-tags=prod`, `make audit` ALL PASSED (CPM80=141µs CPM200=383µs within gate; backup-db-test 6/6). **Next physics candidates still at 0%:** `scoping.go` (WBS scoping engine) + `equipment_validator.go`.
- **2026-06-02** [`eca0eb7` → `origin/main`] **`make lint` cleanup — repo is now golangci-lint-clean (0 issues) + `.golangci.yml` added.** CI's `lint-go` job runs gofmt + `go vet` + govulncheck (NOT golangci-lint), and `make audit` doesn't include `make lint` either, so 10 real golangci-lint findings had accumulated unseen. All fixed, none behavioral: **errcheck** — `writePrivatePEM`/`writePublicPEM` in `cmd/buildos-fork-init/main.go` swallowed `f.Close()` on key files (a swallowed close can silently truncate a PEM); now `pem.Encode` then `return f.Close()` to surface flush errors, with `_ = f.Close()` on the encode-error branch. **staticcheck** — `ST1023` redundant `io.Reader` type on `randSrc` (now `io.Reader(rand.Reader)`); `QF1008` dropped the embedded-field selector `claims.Claims.Validate` → `claims.Validate` in `internal/auth/token.go`; `QF1001` De Morgan in `internal/service/setup.go`'s `looksLikeRegion`/`isShortCode` char-class loops — kept readable by extracting the positive `allowed := …` predicate (silences the linter without the harder-to-read negated-conjunction rewrite). **unused** — deleted dead `writeNotImplemented` (`internal/api/response.go`; every endpoint returns real data since Projects CRUD landed), dead `generateLinearGraph` bench helper (`internal/physics/cpm_test.go`; benchmarks use `generateDiamondGraph`), and the unused `fakeProcurementRecommender` double + its now-orphaned `internal/ai` import (`internal/service/procurement_test.go`). Added **`.golangci.yml`** (v2 schema, default "standard" linters) excluding `web/node_modules` — some JS deps (flatted) ship a vendored `.go` file that the `./...` walk lints; it's gitignored so it never reaches CI's lint-go checkout (verified `git check-ignore`), this just keeps local `make lint` noise-free post-`npm install`. Verify green: `make lint` → **0 issues**; gofmt clean, `go vet ./...` + `-tags=prod` clean, `go build` + `-tags=prod`, affected-package tests, `make audit` ALL PASSED (CPM80/200 within gate; backup-db-test 6/6). **Follow-up (carryover):** wire `make lint` into CI's `lint-go` job now that the repo is clean — needs the `workflow` OAuth scope (push from a user clone; same constraint as the `ci.yml` carryover in Tier 0).

- **2026-06-02** [`fcfa6a3` → `origin/main`] **`ScheduleService` DB-free validation + Gantt-assembly unit tests (test-only, zero production change; extends the `ProjectService` coverage pass below).** `internal/service/schedule.go` was the second service with no service-level test — its validation was only exercised through the API handler fakes. Both `ListProjectTasks` (status enum) and `UpdateTask` (`percent_complete` 0..100 + status enum) validate **before** `pgx.BeginTxFunc`, so the rejection paths are unit-testable with `NewScheduleService(nil, nil, nil, nil)` (also exercises the nil-audit → noop fallback). The CPM round-trip (recalc + persist + River enqueue) stays in the integration suite. New `internal/service/schedule_test.go`: `errors.Is(ErrInvalidInput)` for `ListProjectTasks` unknown-status + `UpdateTask` (percent below/above + unknown status); a table test for the pure `isValidTaskStatus` (the three valid {pending, in_progress, completed} + the case-sensitivity / space-vs-underscore / empty rejections that pin it to the schema CHECK); and three tests for the pure `ganttFromTasks` — **never-computed** (zero `ProjectEnd`, non-nil empty `CriticalPath` so the JSON stays a stable `[]` and the frontend can prompt `/recalculate`), **critical-path emitted in input/WBS order** (helper appends as it walks, no re-sort), and **`ProjectEnd` = max `early_finish`** with a nil-`EarlyFinish` row interleaved (must be skipped, not panic). Verify green: gofmt clean, `go vet`, `go build ./...` + `-tags=prod`, `make audit` ALL PASSED (CPM80/200 within gate; backup-db-test 6/6).
- **2026-06-02** [`18048d0` → `origin/main`] **`ProjectService` DB-free validation unit tests — closed a real service-layer coverage gap (test-only, zero production change).** `internal/service/projects.go` had **no** service-level test: its validation was only ever exercised indirectly through the API handler tests (which use a fake `ProjectServicer`), and the store integration tests cover only the store — so the service's own validation rejection paths + pure helpers were unverified. Confirmed every rejection in `ListProjects` / `GetProject` / `CreateProject` / `UpdateProject` returns `fmt.Errorf("%w: …", ErrInvalidInput)` **before** `pgx.BeginTxFunc` touches the pool, so the whole validation surface is unit-testable with a `NewProjectService(nil, nil, nil)` (which also exercises the nil-audit → `NewNoopAuditRecorder()` fallback) — no DB, mirroring the existing `pipeline_test.go` early-return-validation pattern. New `internal/service/projects_test.go`: table-driven `errors.Is(err, ErrInvalidInput)` assertions for List (nil org / unknown status), Get (nil org / nil project / both nil), Create (nil org / blank+empty name / GSF below-floor + above-ceiling), Update (nil org / nil project / no-fields-set / blank name / unknown status / GSF out-of-range), plus direct coverage of the pure helpers: `paginate` (zero/negative→defaults, perPage clamp to `maxProjectPageSize`, page-2/3 offset math, exactly-max), `validateGSF` (nil-ok + the four boundaries 1499/1500/6000/6001), and `normalizeOptionalString` (nil/blank/empty→nil, surrounding-whitespace trim). `ErrInvalidInput` is the shared sentinel defined in `budget.go:27`. Verify green: gofmt clean, `go vet`, `go build ./...` + `-tags=prod`, `make test`, `make audit` ALL PASSED (CPM80=185µs CPM200=477µs; backup-db-test 6/6).
- **2026-06-02** [`ebd3012` → `origin/main`] **`SyncStatusScreen` goldens — pixel coverage for the field app's primary ops screen (extends the golden harness below).** New `mobile/test/sync_status_screen_golden_test.dart` golden-locks the full-screen layout (FbSyncChip header + last-sync Card + queued-count Card + conditional "up to date"/error copy + the primary "Retry now" button) for the two main variants: **online · synced · empty queue** (gableGreen retry button enabled, "everything up to date") and **offline · queued · error** (amber chip, safety-red "Sync failed" text, queued count "3"). Same opt-in `@Tags(['golden'])` + `RUN_GOLDEN_TESTS` env-guard as the chip goldens. **Determinism:** a fixed phone viewport (`view.physicalSize 1080×2280 @ dpr 3` → 360×760 logical, reset in tearDown), a fixed **local** `DateTime(2026,1,15,9,41)` (the screen calls `.toLocal()` before formatting, so a local — not UTC — instant keeps the timestamp host-TZ-independent), `Intl.defaultLocale='en_US'`, and a `_FixedSyncController` Notifier override pinning `SyncUiState`. **Caught in review:** the first harness omitted `theme: buildFieldTheme()` → the screen rendered on the default *light* Material theme (white canvas, indigo button); fixed to wrap in the real Industrial-Dark theme (deepSpace canvas, gableGreen button). 2 baselines committed under `mobile/test/goldens/`. Verify green: `--tags golden` (all 5 goldens — 3 chip + 2 screen) pass against baselines; `dart format` clean; `flutter analyze` no issues; plain `flutter test` → **12 passed, 6 skipped** (5 golden + 1 live).
- **2026-06-02** [`6500e01` → `origin/main`] **Flutter golden tests for `FbSyncChip` — pixel coverage for the field app's offline/sync states (NEXT_STEPS Tier 3 #8 carryover; test-only, zero production change).** The chip's design contract is "status is never color-only" — each state pairs a colored token with explicit copy — but the existing `fb_sync_chip_test.dart` only asserts on text (`find.text(...)`), so a regression that broke the *dot/spinner* or its *color* would pass unnoticed. New `mobile/test/fb_sync_chip_golden_test.dart` golden-locks the three discrete states wrapped in the real `buildFieldTheme()`, reusing the existing `ProviderScope` stream-override harness: **online + empty queue** → gableGreen dot + "Online"; **offline / queued** → amber dot + "Offline · 3 queued"; **syncing** → blueprintBlue `CircularProgressIndicator` + "Syncing…" (a `_SyncingController` Notifier override forces the branch; the spinner is an infinite animation so the test pumps a **fixed 32ms** frame instead of `pumpAndSettle`, which would hang). Baselines committed under `mobile/test/goldens/` (3 PNGs). **Opt-in by design — the key engineering call:** goldens are renderer-version sensitive and the CI `mobile` job runs `flutter test` against `subosito/flutter-action@v2` with an **unpinned `channel: stable`**, so committed pixel baselines would eventually flake the lane on a Flutter bump (and the OAuth token can't push workflow changes to pin it). So the file is `@Tags(['golden'])` + env-guarded (`RUN_GOLDEN_TESTS=1`), self-skipping under the default run exactly like the `live` tag's env-guard — keeping the backend-free suite deterministic. Run: `RUN_GOLDEN_TESTS=1 flutter test --tags golden`; refresh: append `--update-goldens`. Tag registered in `mobile/dart_test.yaml` with the rationale. Verify green: `--update-goldens` generated 3 baselines → clean `--tags golden` re-run passes against them; `dart format --set-exit-if-changed` clean (38 files, 0 changed); `flutter analyze` no issues; plain `flutter test` → **12 passed, 4 skipped** (3 golden + 1 live self-skip), suite stays green offline.
- **2026-06-02** [`2f0b63c` → `origin/main`] **Backup automation + DR runbook (NEXT_STEPS Tier 2 #5) — closes the per-fork backup/restore gap.** The single-tenant-per-fork model (ADR-002) makes backup a per-fork operational concern, and there was **zero** backup automation. Shipped **`scripts/backup-db.sh`**: `pg_dump -Fc` (compressed custom format, `--no-owner --no-privileges` so a restore into a fresh-role fork DB works) → `BACKUP_DIR/buildos-<db>-<UTC>.dump` + a `.sha256` integrity sidecar; optional storage-agnostic upload via `BACKUP_UPLOAD_CMD` (literal `{file}` substituted — no cloud SDK is a hard dep); retention pruning keyed on the **filename-embedded UTC timestamp** (not mtime, so syncing dumps around can't change what survives) — drops dumps older than `BACKUP_RETENTION_DAYS` (30) but always keeps a floor of `BACKUP_RETAIN_MIN` (7) most-recent (a stalled cron can never erase the last good copy); `--prune-only` / `--no-prune` modes; a `BACKUP_NOW_EPOCH` clock seam for deterministic tests. GFS tiering is intentionally pushed to object-store lifecycle rules, not reimplemented in bash. **`scripts/restore-db.sh`**: verifies the `.sha256` sidecar and **aborts on mismatch** (a corrupt backup must never half-restore over a live DB), a destructive-op guard refusing without `--confirm`/`BACKUP_RESTORE_CONFIRM=1` (mirrors the migration-linter opt-in posture), then `pg_restore --clean --if-exists`. **`scripts/backup-db.test.sh`**: DB-free regression suite (mirrors `lint-migrations.test.sh`) — 6 cases over a fixed clock + fixture dirs: retention keeps-recent / prunes-old (incl. sidecar removal) / floor-keeps-newest-N, and restore guards no-confirm / missing-dump / checksum-mismatch (all bail before `pg_restore`). Wired into `make audit`. **`docs/dr-runbook.md`**: RPO/RTO table, cron + k8s CronJob scheduling, object-store lifecycle GFS note, quarterly restore-drill checklist, and a failure playbook. Makefile targets `backup-db` / `restore-db` (DUMP=…/CONFIRM=1) / `backup-db-test`; `.gitignore` now excludes `backups/` + `*.dump`. Verify green: `make audit` ALL PASSED (backup-db-test 6/6; CPM80=184µs CPM200=457µs); real round-trip smoke against the local docker DB — `backup-db.sh` produced a 108K dump + verifying checksum, `restore-db.sh` refused without `--confirm` (rc=3) and completed a confirmed `pg_restore` round-trip (rc=0).
- **2026-06-02** [`0fdd6e1` → `origin/main`] **Native AI surface test-hardening — closed the end-to-end vault→AI coverage gap (test-only, zero production behavior change).** A readiness assessment of `internal/ai` found two coverage gaps on the standalone AI path: (1) the `DailyBriefing` HTTP handler was never exercised (only `RecommendScheduleAdjustments` had handler tests), and (2) **no test wired the REAL encrypted vault as the `ai.KeyResolver` through to an actual AI call** — every existing AI test used a `stubKeyResolver`, so the full decrypt→wire path was unproven. **(1)** Added 5 `DailyBriefing` handler tests to `internal/api/agents_test.go` against the existing `mockAgentsService` (the mock already carried `briefingResult`/`briefingErr` + `GenerateDailyBriefing`): happy-path (asserts the service receives `callerOrgID`/`sub`/`role` from JWT claims + the `{briefing:{reply,task_count,alert_count}}` envelope), and the four soft-fail error maps — `ai.ErrUnconfigured`→503, `ai.ErrTransient`→502, `ai.ErrRateLimited`→429, and a bad-stored-key `&ai.HTTPError{StatusCode:401}`→503 (the BYOK-misconfigured path). **(2)** New `internal/service/ai_vault_integration_test.go` (`//go:build integration`, `TestAIVault_EndToEnd`) builds a **real `VaultService`** (Testcontainers Postgres via the existing `newVaultService(t)` helper — 32-byte zero master key, real `cryptobox` cipher, seeded org) and passes it as the `ai.Client`'s `KeyResolver`, pointed at a fake Anthropic `httptest` upstream (no real key, fully deterministic). Proves, in order: **no key** → `ai.ErrUnconfigured`, upstream NEVER called, no metric recorded; **after `SetCredential(anthropic, "sk-ant-vault-e2e-7777")`** → 200 with the *vault-decrypted* key on the `x-api-key` header (the complete decrypt→wire path), `resp.Reply` non-empty, and a `MetricsObserver` stub records exactly one call (`kind=daily_briefing`, fast model `claude-sonnet-4-5`, nil err); **other org's ctx** → `ai.ErrUnconfigured` with no extra upstream hit (org isolation — one org's key never resolves for another). Verify green: `go build ./...` + `-tags=prod`, `go vet`, gofmt clean, `make audit` (ALL PASSED; CPM80=180µs CPM200=484µs), `go test ./internal/api/...` (DailyBriefing handler tests), full `go test -tags=integration ./internal/service/...` (49s, incl. the new vault→AI round-trip).
- **2026-06-02** [`520758a` → `origin/main`] **`GET /api/v1/capabilities` backend gap closed + proactive AI/email gating wired + BYOK live journey.** Was a documented gap: the web client shipped the full capabilities client (`api/endpoints/capabilities.ts`, `state/capabilityStore.ts`) but the endpoint was never mounted, `refreshCapabilities()` was never called, and the store ran in permanent assume-on fallback. **Backend** — new `VaultService.Capabilities(ctx, orgID)` derives `ai_configured` / `email_configured` purely from **active-credential presence** in the BYOK vault (reuses `store.ListByOrg` via the read-only tx pattern — no new store query; never decrypts — `last4` is already plaintext metadata). New `MountCapabilitiesRoutes` mounts `GET /api/v1/capabilities` **auth-only (any authenticated role, NOT admin-gated)** — every role's UI gates AI/email affordances on these flags — wired in `router.go` inside the existing `if integrations != nil` block (so it mounts only when the vault is wired; nil → frontend keeps assume-on). Response `200 {data:{ai_configured, email_configured, providers[]}}` where each provider entry is `{provider, configured, fingerprint(last4), created_at, created_by}` (fingerprint/created_* omitted when unconfigured). 5 handler tests (`internal/api/integrations_test.go`, new — both-off / anthropic-on / both-on providers-shape / 401-no-claims / 500-no-leak) + 2 service integration tests (`integrations_integration_test.go` — set→flip-on→delete→flip-off, org isolation). Contract documented in `API_CONTRACT.md` §13. **Frontend** — `main.ts` fires `void refreshCapabilities()` after `initSession()` (fire-and-forget, never blocks boot); `fb-integrations-page.ts` calls it after every save/toggle so AI/email screens reflect the flip immediately; dropped the "backend gap / NOT mounted" comments in `capabilityStore.ts` / `capabilities.ts` / `types/models.ts` (assume-on kept as a defensive path — a capabilities outage must never brick the UI). New `web/tests/capabilities.test.ts` (3 units — populates ai/email from success, keeps assume-on when the fetch throws). **Live journey** — `web/tests/live/settings-integrations.live.spec.ts` (sorts AFTER onboarding so the owner exists; env-guarded skip): sign in → nav-click to Settings → Integrations → assert the "AI features are off…" banner → fill a **FAKE** Anthropic key (`sk-ant-e2e-fake-key-0000`) → Save → `waitForResponse` on `GET /capabilities` asserts `ai_configured===true` (deterministic — backend treats AI-on as key-presence, no upstream Anthropic call) + "Anthropic key saved." toast + banner gone → toggle off (force-click the visually-hidden `fb-switch` input under its `.track` overlay) → `ai_configured===false` + banner returns (cleanup restores the org for serial isolation). **Two infra fixes this surfaced:** (1) the live 404 root cause was a STALE `go run` child binary holding :8080 from a prior harness run — the harness killed only the `go run` parent, leaking the compiled child; fixed `scripts/e2e-backend.sh` to launch the server via `setsid` (own process group) and `kill -- -PGID` the whole tree on teardown (verified: port frees post-run). (2) the toggle-off click was intercepted by `fb-switch`'s `.track` span over its `opacity:0` input — `force:true` click. Verify green: `go build ./...` + `-tags=prod`, `go vet`, `make audit` (ALL PASSED; CPM80=185µs CPM200=468µs), `go test -tags=integration` capabilities round-trip; web `eslint`+`prettier`, `tsc --noEmit`, 162 vitest, `vite build`; all **4** live specs pass end-to-end through the harness (onboarding ×2 + recalc + settings-integrations).
- **2026-06-02** [`630010a` → `origin/main`] Live recalc → CPM cascade-diff E2E journey + a real nav bug it caught. New `web/tests/live/recalc-cascade.live.spec.ts` (runs in the same serial live config, AFTER onboarding): signs in as the owner the onboarding journey claims → opens the schedule workspace **via the nav-rail link click** (client-side nav, NOT `page.goto` — a full reload drops the in-memory access token and bounces to `/login`) → asserts the seeded "E2E Tower" is auto-selected → "Schedule not computed yet" empty state → clicks Recalculate → asserts the Gantt draws, "recomputed in {N}ms" surfaces (`recalculation_ms`), and the "Critical path changed" cascade notice renders (the seeded linear FS chain makes every task critical). Harness: new `--seed-schedule` mode in `scripts/e2e-backend.sh` plants the project + a 3-task linear FS chain (Site Prep→Foundation→Framing, durations 3/5/8) with NO CPM results and leaves onboarding INCOMPLETE (so the onboarding spec's wizard still runs); exports `E2E_SCHEDULE_PROJECT_NAME`. The `e2e` CI lane now boots with `--seed-schedule`. **The genuine bug this journey caught (ship-blocking, now fixed):** the nav rail was completely dead for navigation — `fb-app` never wired the `@navigate` event from `fb-org-shell`. `fb-nav-item` renders a real `<a href>` but `preventDefault`s and emits a composed `navigate` event instead; the router's document-level click interceptor can't see the anchor because it lives in `fb-nav-item`'s shadow root (the event is retargeted to the host, so `closest('a[href]')` misses it). Fixed by adding `@navigate=${this.onNavigate}` + an `onNavigate` handler to `fb-app.ts` that drives the SPA router. Locked in backend-free with a regression unit test (`web/tests/app.test.ts` — mounts `fb-app` authenticated via a mocked `login()`, dispatches a composed `navigate` event at `fb-org-shell`, asserts the route changes). Web gate green: eslint+prettier, `tsc --noEmit`, 159 vitest, `vite build`; all 3 live specs pass end-to-end through the harness (onboarding ×2 + recalc).
- **2026-06-02** [`a2ae6db` + `52daf73` → `origin/main`] Backend-dependent E2E harness — the ▶ NEXT live-backend lane, now closing. Stood up `scripts/e2e-backend.sh` (boots a REAL `cmd/server` under native auth + vault against a migrated, seeded Postgres; `--db-up` for local Docker, bare for a CI service container; `--seed-field` seeds an onboarding-COMPLETE org + project + task and exports the `E2E_*` contract). Two live lanes wired into `.github/workflows/ci.yml`: **`e2e`** (web Playwright — first-run claim → 6-step wizard → portfolio) and new **`e2e-mobile`** (Flutter `test/live/sync_live_test.dart` — claim → offline queue → reconnect → drain → server-side idempotency replay 409 → `pull()` cursor advance). The mobile lane self-skips offline (`@Tags(['live'])` + env guard) so the backend-free `mobile` job stays green. **Two genuine native-auth bugs the live lane surfaced (both ship-blocking, now fixed):** (1) `internal/store/field.go LookupUserIDBySubject` keyed on the legacy `oidc_subject` column (NULL for every native user) while the native JWT `sub` IS `users.id` — the entire mobile field write path (progress/checkin/daily-log) 404'd. Fixed to resolve by `id`. (2) `internal/store/feed_cards.go ListFeedCards` resolved user-targeted cards through the same `oidc_subject` subquery — user-targeted feed cards never matched native callers. Fixed to compare `target_user_id` against the parsed subject UUID directly. Also fixed the mobile `ApiClient._unwrap` which never unwrapped the `{data,meta}` envelope (broke every typed data call). `testdb.SeedUser` now seeds NULL `oidc_subject` to model real native users and stop masking this bug class. Verify green: `go build ./...` + `-tags=prod`, `go vet`, `make test`, `make test-prod`, full `make test-integration`, gofmt + `dart format` + `flutter analyze` clean; live mobile lane passes end-to-end through the harness; offline `flutter test` → 12 pass / 1 skip.
- **2026-06-01** [`dc78aa7` → `origin/main`, direct push, no PR] Projects CRUD — replaced the four `writeNotImplemented` stubs in `internal/api/projects.go` with real handlers wired through the canonical trio (handler → `ProjectServicer` interface → `service.ProjectService` → stateless `store.ProjectStore`). **Store** (`internal/store/projects.go`): added `ProjectStore` CRUD next to the existing `VerifyProjectInOrg` helper — `ListByOrg` (optional status filter, `created_at DESC, id DESC`, LIMIT/OFFSET), `GetByID` (cross-org → `ErrNotFound`, no existence leak), `Create` (INSERT, DB applies `status='active'` default + timestamps), `Update` (COALESCE partial patch on name/address/status/gsf, `updated_at=now()`, scoped by id+org). Nullable `address` scanned through a `*string` local so SQL NULL → `""`. **Service** (`internal/service/projects.go`, new): one-tx-per-mutation + audit (`project.created` / `project.updated`), read-only tx for List/Get; validates closed status enum {active, completed, archived}, GSF range [1500,6000] when present, name non-blank; `paginate` clamps per_page to [1,200] and page≥1; create defaults status (not settable), update requires ≥1 field; dates not editable on update (create-only per API contract). **Handler** (`internal/api/projects.go`): `List` (?status/page/per_page, malformed paging → 0 → service defaults, stable `[]`), `Create` (parses `permit_issued_date`/`project_start_date` via shared `parseOptionalDate`, 400 on bad date, 201), `Get`/`Update` (parse `projectID` from URL, 404 on miss). `writeProjectError` maps `ErrNotFound`→404, `ErrInvalidInput`→400, default→500 (no DB leak). Wired `ProjectService` into `RouterConfig` + `cmd/server/main.go`; routes/RBAC were already in `router.go` (List/Get any authed; Create/Update owner/admin). Tests: 17 handler units (`internal/api/projects_test.go`, fake `ProjectServicer` — success/validation/not-found/bad-JSON/bad-date/401/500-no-leak/stable-`[]`) + 7 store integration tests (`internal/store/projects_integration_test.go`, `//go:build integration` — create round-trip, null address, cross-org get/update → NotFound, status filter + org isolation, pagination, COALESCE partial patch). Verify green: `go build ./...`, `go vet ./...`, unit tests, store integration suite (8.1s), `make build-prod`. Reused existing `parseOptionalDate` (financials.go) and `strPtr` (setup_integration_test.go) to avoid redeclaration.
- **2026-06-01** [`dc78aa7` → `origin/main`, direct push, no PR — committed together with Projects CRUD above] Standalone pivot (native-stack cutover, two waves). Abandoned the Brain hub-and-spoke. **Native auth** (`internal/auth` + `internal/service/auth.go`): email/password with argon2id, BuildOS-issued RS256 JWTs (`iss=buildos`, `aud=buildos`), server-revocable opaque refresh tokens, bootstrap-token first-owner claim at `POST /api/v1/auth/claim`, password reset via Resend. **Native AI** (`internal/ai`): direct Anthropic Messages API client (BYOK), default model claude-opus-4-6 / fast claude-sonnet-4-5, circuit breaker, image input, soft-fail `503` when no key. **Encrypted vault** (`internal/cryptobox` + `internal/service/VaultService`): AES-256-GCM BYOK store keyed by `VAULT_MASTER_KEY`, admin-gated `/api/v1/integrations` surface (metadata only, never secret bytes). **Mailer** (`internal/mailer`): Resend transactional email. Procurement vendor review now creates a local `vendor_review_requested` feed card (`201 {feed_card_id}`) instead of emitting A2A. **Wave 2 deletion**: removed `internal/brain`, `internal/a2a`, `internal/a2asigner`, billing, `cmd/dev-idp`; `migration 013` drops `a2a_inbound_log` + `a2a_outbound_dlq`; `cmd/buildos-fork-init` now emits JWT keypair + vault master key + bootstrap token (no JWKS/Brain registration). `go.mod` `replace ... futurebuild-brain` removed. Docs refreshed (CLAUDE.md, API_CONTRACT.md, TECH_STACK.md, this file). Build/vet/test green in both default and `-tags=prod`; migration linter + regression suite pass.
- **2026-05-06** PR #26 [`4573379`] S4 9.1 follow-up — `RecommendScheduleAdjustments` HTTP handler + RBAC. Closes the loop on PR #25's service method by exposing it over HTTP. New route `POST /api/v1/projects/{projectID}/schedule/recommend-adjustments` mounts under the existing `/schedule` sub-route, double-gated: `RequireMinRole(RoleSuperintendent)` (CPM-affecting; matches `/recalculate`'s gate) + `RequirePlanTier(PlanTierPro)` (consumes metered AI tokens). Conditional on `cfg.AgentsService != nil`, matching the pattern already in place under `/api/v1/agents/*` so the worker binary doesn't fail to start. `AgentsServicer` interface extended with `RecommendScheduleAdjustments`. Handler reads `project_id` from URL + caller org/sub from JWT claims; returns the full `ScheduleAdjustmentSet` (`run_id`, `tokens_used`, `cost_cents`, `currency_code`, `adjustments[]`, `applied_deltas`, `skipped_rationale_only`) on 200. Error map: `400 VALIDATION_ERROR` (invalid project_id / `ErrInvalidInput`); `404 NOT_FOUND` (`ErrNotFound`); `503 SERVICE_UNAVAILABLE` (`ErrAgentsMaestroUnavailable` / `ErrAgentsScheduleServiceUnavailable` — worker-binary path; tells callers to retry against server binary rather than treating as permanent input error); `502 UPSTREAM_ERROR` (`brain.ErrTransient` / Brain 5xx); `401 UNAUTHORIZED` (Brain rejected token). Subtle: the service's `"apply succeeded; recalc deferred"` path returns `(result, err)` where deltas were persisted but post-tx CPM re-run failed — handler detects via non-zero `RunID` on the result and returns `200` with the body (reporting 5xx would mislead the caller into thinking deltas weren't applied; next `/schedule/recalculate` re-runs CPM). New `internal/api/agents_test.go` with 7 tests: happy-path (asserts service args + envelope shape: run_id, applied_deltas, cost_cents, currency_code), bad project_id → 400, `ErrNotFound` → 404, `ErrInvalidInput` → 400, both 503 sentinels, `brain.ErrTransient` → 502, and the recalc-deferred regression guard (asserts 200 with applied_deltas in body). `make audit` ALL PASSED. CPM80=144µs, CPM200=374µs.
- **2026-05-06** PR #25 [`2426c2b`] S4 Session 9.1 — DailyFocusAgent calls Maestro `update_schedule` (closes the first agent-driven CPM-edit flow under the ADR-003 two-event constraint). New typed Maestro task `update_schedule` in `internal/brain/maestro_tasks.go` (`UpdateScheduleRequest{ProjectID, ProjectStartDate, Tasks[], Dependencies[]}` + `UpdateScheduleResponse{CostMetadata + ScheduleAdjustment[]}` where `ScheduleAdjustment.NewDurationDays *int` so rationale-only recommendations serialize cleanly). Validates `ProjectID` non-zero + tasks non-empty client-side. `internal/store/schedule.go` extends `UpdateTaskParams` with `DurationDays *int` (operator path leaves nil — DHSM-derived; only the agent path writes through it) + `COALESCE($6, duration_days)` in the UPDATE. `internal/service/agents.go` adds `MaestroScheduleAdjuster` interface, extends `AgentsService` with `scheduleStore`+`scheduleService`+`scheduleAdjuster`, constructor signature now `(pool, fields, feed, scheduleStore, scheduleService, briefer, adjuster, audit)`. New `RecommendScheduleAdjustments(ctx, callerOrgID, callerUserSub, projectID)`: single tx wraps `VerifyProjectInOrg` → `GetProjectTasks/Dependencies/StartDate` → `Maestro.UpdateSchedule` (held inside tx, mirrors `ProcurementService.RecommendVendors` pattern so recommendation+audit+duration writes commit/rollback atomically) → loops `Adjustments`, applies via `UpdateTask` (skips nil/negative `NewDurationDays`; defensive guard against a buggy Brain response) → batch audit row `schedule.maestro_edit` / `AuditResourceSchedule` keyed by `project_id` with metadata `{run_id, tokens_used, cost_cents, currency_code, recommended_delta_count, applied_deltas, skipped_rationale_only, adjustments[]}`. After tx commit, synchronously calls `ScheduleService.RecalculateSchedule` (its own tx) so CPM physics re-validates with the new durations; on recalc failure returns the result + wrapped `"apply succeeded; recalc deferred"` error (eventual-consistency degradation — durations + audit are persisted, physics catches up at next manual recalc). Skip when `AppliedDeltas == 0` to avoid double-audit on no-op. Two new sentinels: `ErrAgentsMaestroUnavailable` (nil adjuster — worker binary doesn't expose agent endpoints) + `ErrAgentsScheduleServiceUnavailable` (nil scheduleStore/scheduleService). `cmd/server/main.go` wired with the new 8-arg signature; worker passes nil for the schedule trio. ADR-003 note: this is the OUTBOUND BuildOS → Brain Maestro call — Brain's reply rides the synchronous HTTP envelope; the inbound-A2A two-event constraint applies to round-trips this flow does not exercise. Tests: `maestro_tasks_test.go` validation matrix + round-trip (envelope discriminator, project_id, FS dependency, two adjustments incl. rationale-only with nil pointer); `agents_test.go` constructor migration + new validation matrix (nil org, nil project, nil adjuster → sentinel, nil schedule trio → sentinel). One follow-up gofmt commit (`3c323f9`) for indentation alignment after the initial push. `make audit` ALL PASSED. CPM80=184µs, CPM200=460µs.
- **2026-05-06** Gate 2 ratified by owner. New [ADR-003](./.agents/handoff/ADR-003-gate-2-ratification.md) codifies the decision: ADR-001 D14 (LocalBlue auto-flow) ACCEPTED unconditionally; ADR-001 D7 (A2A receiver scope) ACCEPTED with payload-alignment caveat (Option A — envelope + event-type list locked, per-event payload schemas deferred). Status lines added inline at ADR-001 D7 and D14. Four Brain-side backlog items surfaced in "Cross-repo coordination" below (P0 OrgID, P0 LocalblueLeadCapturedPayload re-derivation, P1 create_feed_card target_role, P2 Issuer cutover). S4 Session 9.1 unblocked with a two-event constraint (`update_schedule` + `delivery_confirmation` only). Docs-only diff; no production code touched.
- **2026-05-05** PR #23 [`73b6b87`] S3 Session 8.2 — LocalBlue `lead.captured` inbound test surface (`internal/service/a2a_integration_test.go`). Test-only; no production code touched. Pairs with PR #22's receiver-side handler tests to lock the inbound A2A contract before Gate 2. Existing happy-path test covered prospect shape (LEAD stage, `localblue:` source tag, probability_pct=10) and feed-card shape (urgent, owner-targeted); existing `_RejectsMissingContractorOrg` covered the org-resolution gate. Six new tests close the remaining gaps: (1) `_IdempotencyReplay` — same envelope twice → `second.AlreadyProcessed=true`, exactly one prospect + one feed card (load-bearing guarantee against duplicate-prospect pipeline corruption on Brain retry); (2) `_RejectsMissingLeadName` — `ErrInvalidInput` + FULL rollback assertion (zero `a2a_inbound_log`, zero prospect, zero feed_card) so Brain's retry can succeed once the upstream payload is corrected (without rollback the lead would be lost forever); (3) `_RejectsMissingContactName` — symmetric; (4) `_OptionalFieldsMapToNull` — pins the `stringPtrOrNil` contract: empty contact_email/contact_phone/address arrive as SQL NULL not "" (downstream IS NULL semantics + future uniqueness gates); (5) `_DefaultSourceTag` — pins the `localblueSourceTag` boundary: empty source produces `"localblue"` not `"localblue:"` (no dangling colon corrupting analytics buckets); (6) `_NoPipelineStoreReturnsError` — defensive nil-pipelineStore guard returns `ErrInvalidInput` rather than nil-deref panic, with full rollback so retry semantics stay coherent if pipelineStore is wired later. Atomicity assertions share the `assertNoDedupOrOrphans` helper. `make audit` ALL PASSED. CPM80=183µs, CPM200=439µs. **Triggers Gate 2** — owner approval requested on (a) the LocalBlue auto-flow per ADR-001 D14 and (b) ratification of inbound A2A event schemas as cross-repo wire contracts.
- **2026-05-05** PR #22 [`0fdfac1`] S3 Session 8.1 — A2A receiver idempotency replay + 1 MiB body cap tests (`internal/api/a2a_test.go`). Test-only; no production code touched. (1) `TestA2AHandler_IdempotencyReplaySameFeedCardID` — stateful `idempotencyReplayService` mock mirrors the production `ProcessWebhook` dedup contract: first envelope mints a `feed_card_id` with `already_processed=false`; second delivery with the same `IdempotencyKey` returns the SAME `feed_card_id` with `already_processed=true`. Asserts both 200 responses, both flag values, and `feed_card_id` stability — the user-facing guarantee that Brain's at-least-once retries don't duplicate feed cards. Decode shape now drills the `{data, meta}` response envelope properly. (2) `TestA2AHandler_BodyTooLargeReturns413` — > 1 MiB POST body trips the `mw.A2AInboundMaxBodyBytes` cap. Wraps `r.Body` with `http.MaxBytesReader` directly (the same wrapper `mw.MaxBodySize` middleware installs on the route group at `router.go:176-177`) so the handler-only test exercises the 413 branch without the chi stack; asserts `413 PAYLOAD_TOO_LARGE` rather than the generic 400 the handler emits on JSON decode errors. Unknown `event_type` → `400 UNKNOWN_EVENT` was already covered by `TestA2AHandler_ServiceErrorMaps/unknown_event` (not duplicated). All 8 `TestA2AHandler_*` subtests pass. `make audit` ALL PASSED. CPM80=179µs, CPM200=480µs. Sets up Session 8.2 (LocalBlue `lead.captured` inbound; Gate 2 trigger).
- **2026-05-05** PR #21 [`e2f2f5f`] Vault SecretSource backend (Tier 2 #4). New `internal/config/vault.go` implements the `SecretSource` interface for HashiCorp Vault KV v2 — the production-default secret store for customer forks. Spec format: `vault://<mount>/data/<path-prefix>` (e.g. `vault://kv/data/buildos/acme`). One Vault secret per logical key with a single `value` field, provisioned by operators via `vault kv put kv/buildos/<fork>/<KEY> value=...`. KV v2 only — KV v1 mounts (no `/data/` segment) rejected at construction with a clear error so the spec format and the underlying HTTP API path agree literally. Address/TLS via SDK `DefaultConfig` (`VAULT_ADDR`, `VAULT_CACERT`, …); auth in priority order: `VAULT_TOKEN_FILE` (BuildOS-specific extension for k8s projected service-account tokens) → `VAULT_TOKEN` env → `~/.vault-token` (SDK default). Empty/expired token at construction permitted — first lookup fails loud rather than refusing to start, the right ergonomic for operators staging a fork. Per the SecretSource contract: hit returns (val, true, nil); 404, empty value, and KV v2 tombstoned versions all return ("", false, nil) (clean miss); transport / auth / 5xx errors propagate so a chain short-circuits ("Vault is down" must NOT silently fall back to env). `LoadSecretSource` gains a `vault://` dispatcher so chain composition works (`chain:vault://kv/data/buildos/acme,env`); the legacy "unknown spec rejected" test case migrated from `vault://prod` (now ambiguous) to `azure-kv://prod`. 11 unit tests using a `httptest` stub of the KV v2 API (spec validation matrix, token-file resolution, hit/404-miss/empty-value-miss/wrong-field/tombstone/non-string-value/empty-key, transport-error propagation, Close clears in-memory token). 1 integration test (build tag: integration) stands up `hashicorp/vault:1.18` dev container, provisions DATABASE_URL + BRAIN_JWKS_URL + BRAIN_ISSUER_URL via SDK `KVv2.Put`, and round-trips through both direct `LookupSecret` and the full `LoadWithSource` path — confirms the spec format maps cleanly to a real Vault. New deps: `github.com/hashicorp/vault/api v1.23.0` (+ transitive hashicorp/* and mitchellh/* utilities, all Apache-2.0 / MPL-2.0). `make audit` ALL PASSED. CPM80=139µs, CPM200=483µs.
- **2026-05-05** PR #20 [`715af97`] S3 7.2 caller wiring: `ProcurementService.RequestVendorReview` is the first real consumer of the `a2a.Emitter` shipped in PR #19. Opens one tx, verifies item ownership (cross-org isolation via existing `GetProcurementItem`), calls `EmitReviewMaterialQuote` (`InsertTx` on the same tx so enqueue commits/rolls back atomically with audit), records one audit row keyed by `procurement_item_id` (`procurement.vendor_review.requested`), returns the emitter's idempotency key for caller correlation. New `VendorReviewEmitter` consumer interface (defined in procurement package — service doesn't pin the full `*a2a.Emitter` surface). Constructor signature grew to 5 args: pool, items, maestro, emitter, audit. `cmd/server` constructs `*a2a.Emitter` once over the shared River insert client; `cmd/worker` passes nil (daily `RecomputeStatuses` sweep doesn't trigger operator-driven review flows). New `ErrA2AEmitterUnavailable` sentinel mirrors `ErrMaestroUnavailable`. Validation split: service validates org/item/emitter-presence; wire-shape validation (vendor non-empty, total_cents non-negative, currency_code USD/CAD) delegated to emitter, its `ErrInvalidArgs` wrapped as `ErrInvalidInput` so handlers see the uniform service-layer sentinel. Audit metadata captures idempotency_key + structured fields (vendor, total_cents, currency_code, rfq_id, has_reasoning bool — Reasoning text excluded as Confidential narrative). 2 new tests (validation matrix; nil-emitter sentinel); happy-path with real tx deferred to integration suite. **No labor-bid wiring** — BuildOS doesn't have a labor RFQ surface yet (S6 SubLiaisonAgent territory); `EmitReviewLaborBid` stays unused but emitter test coverage from PR #19 keeps it live. `make audit` ALL PASSED. CPM80=182µs, CPM200=423µs.
- **2026-05-05** PR #19 [`9dc9eae`] S3 Session 7.2: outbound A2A emitter package. New `internal/a2a` package (depends on `worker` + `river` only — pointedly NOT `internal/service`, to avoid the import cycle that would form when domain services and `service/a2a.go` both want to call into the emitter). `Emitter.EmitReviewMaterialQuote(ctx, tx, args) (uuid.UUID, error)` and `Emitter.EmitReviewLaborBid(ctx, tx, args) (uuid.UUID, error)` validate inputs, marshal a typed JSON payload, and `InsertTx` a `worker.A2AWebhookDispatchArgs` on the supplied `pgx.Tx` so enqueue commits / rolls back atomically with the surrounding domain mutation. Returns the freshly-minted idempotency key for caller correlation. The actual JWS signing + HTTPS POST happens later in the River worker via the already-shipped `service.A2AOutboundService.DeliverA2AWebhook`. `Enqueuer` interface decouples tests (`fakeEnqueuer` captures the dispatch args) from a real River client; compile-time assertion `var _ Enqueuer = (*river.Client[pgx.Tx])(nil)` keeps the prod-side wiring honest. Wire-shape detail: `rfq_id` is `*uuid.UUID` (not `uuid.UUID`) in the JSON payload so `omitempty` actually drops the field when `uuid.Nil` is passed — Brain's ETL/dedup needs to distinguish "absent" from "nil-uuid". Event-type constants (`review_material_quote`, `review_labor_bid`) duplicated from `service/a2a.go` by design (zero coupling). Validation up-front and pure: nil `org_id`, empty `vendor`/`bidder`, negative amounts, unsupported `currency_code` all return `ErrInvalidArgs` with no tx interaction. 9 test functions: nil-`Enqueuer` panic, validation matrix (table tests, both events), envelope shape, omitempty contract for rfq_id/reasoning/timeline, enqueue-error propagation. `make audit` ALL PASSED. CPM80=181µs, CPM200=440µs. Caller wiring (procurement service → emitter; pipeline service → emitter) lands in a follow-up.
- **2026-05-05** PR #18 [`c6c86c1`] S3 Session 7.1: Procurement Maestro `RecommendVendors` + migration 009. New `MaestroProcurementRecommender` interface (typed `procurement_recommend` task surface) wired into `ProcurementService` via a nil-tolerant constructor — `cmd/server` passes `brainClient.Maestro`; `cmd/worker` passes `nil` (daily `RecomputeStatuses` tick has no recommendation surface, so `RecommendVendors` short-circuits with `ErrMaestroUnavailable`). `RecommendVendors` opens one tx, verifies item ownership via new `GetProcurementItem` store method, calls Maestro with the item's `estimated_cost_cents` as budget context, persists each ranked vendor via new `CreateProcurementRecommendation` store method (all share `resp.RunID`), and writes one batch audit row keyed by `procurement_item_id` with metadata `{run_id, recommendation_count, tokens_used, cost_cents, currency_code}`. New migration `009_procurement_recommendations` follows Composite Currency Pattern (`predicted_spend_cents` + `predicted_spend_currency_code`); `confidence_pct SMALLINT` (0..100) instead of float per no-floats culture — Maestro's float64 `Confidence` is rounded * 100 with NaN/negative/>1 clamping (defends against a buggy Brain response violating CHECK + rolling back the tx). `vendor_id` is nullable, no FK (vendors table not yet modeled). 3 indexes (item+created_at DESC, run_id, org+created_at DESC) all single-line + `-- buildos:lock-ok:` (fresh table). New audit constant `AuditResourceProcurementRecommendation`, model struct `models.ProcurementRecommendation`, sentinel `ErrMaestroUnavailable`. `fakeProcurementRecommender` test double + 3 new tests (validation gate, nil-Maestro path, `confidenceToPct` table test covering edges). `make audit` ALL PASSED. CPM80=183µs, CPM200=431µs.
- **2026-05-05** PR #17 [`62bf293`] S2 Session 5.1: outbound `buildos.prospect_promoted` A2A event on PROSPECT → PROJECT atomic promotion. Extends `transitionToPermitIssued` to enqueue an `A2AWebhookDispatch` River job inside the same tx as the project insert + prospect update + HydrateProject job — Brain only ever receives the announcement for a project that committed. Payload is feed-card-shaped per ADR-001 D14 (`card_type=pipeline.prospect_promoted`, title, body, priority, empty actions, metadata{prospect_id, project_id, org_id, gsf, permit_issued_date RFC3339, optional address}). `address` is omitted when nil so Brain's dedup/ETL distinguishes absent from empty. `EventTypeProspectPromoted` is exported as the cross-repo event_type identifier. New helper + struct are package-private. Tests assert explicit envelope shape (renamed JSON key fails loud) plus the nil-address omission contract. Full transition path stays deferred to integration suite. `make audit` ALL PASSED. CPM80=187µs, CPM200=425µs.
- **2026-05-05** PR #16 [`6ee6752`] S1.5 part 4 / Session 3.4 (closes S1.5; **triggered Gate 1; ratified by owner 2026-05-05**): wire typed `Maestro.DailyBriefing` into `agents.GenerateDailyBriefing` + per-call billing-audit row. `MaestroChatter` (free-form `Chat`) replaced with `MaestroDailyBriefer` (typed `DailyBriefing(ctx, brain.DailyBriefingRequest) (*brain.DailyBriefingResponse, error)`). The local `buildDailyBriefingPrompt` helper is deleted — Brain's `daily_briefing` task owns the prompt template per ADR-001 D5; BuildOS only assembles the structured context (`Tasks`, `Alerts`, `UserRole`). After a successful Maestro call, a short standalone tx writes one audit row with `action="ai.daily_briefing.invoked"`, `resource_type="ai_run"` (new `AuditResourceAIRun` constant), `resource_id=resp.RunID`, and metadata JSON `{run_id, session_id, tokens_used, cost_cents, currency_code, task_count, alert_count, task}`. This is the per-call meter that rolls into the org's monthly AI usage line. Audit is best-effort — `AuditService.Record` log-and-swallow path keeps the briefing from being held back if the audit insert fails. `NewAgentsService` constructor takes `AuditRecorder` (nil → `NoopAuditRecorder`); `cmd/server/main.go` wired. Tests: typed `fakeBriefer` for the new interface, nil-audit-fallback regression, validation-only paths (the three pre-existing `buildDailyBriefingPrompt` tests dropped — function gone). `make audit` ALL PASSED. CPM80=184µs, CPM200=438µs.
- **2026-05-05** PR #15 [`543eac6`] S1.5 part 3 / Session 3.3: Brain Hub credential proxy stubs (ADR-001 D6). New `internal/brain/hub.go` adds `HubClient.GetCredential(ctx, GetCredentialRequest) (*Credential, error)` and `HubClient.RefreshIfExpired(ctx, credentialID) error`. Mode is fork-static via `Config.HubDirectMode` (driven by `BRAIN_HUB_DIRECT` env): proxy mode (default) returns `Credential.ProxyHandle` only — raw secret never enters the fork; direct mode appends `?mode=direct` and Brain returns `Credential.Secret` if its policy gate authorizes the fork. Wired into `Client.Hub` alongside `Maestro` / `Billing` so it inherits S1.5 part 1 retry + breaker + per-method timeouts. `Credential.Secret` is Restricted-class — slog (PR #11) + audit (PR #10) scrubbers already mask it on egress. Validation rejects empty provider / `uuid.Nil` credentialID client-side. Empty Scope auto-defaults to `"default"`. 11 new tests cover both modes, empty-scope default, custom-scope path-escape, 404 → ErrNotFound, 502 → ErrTransient, fork-static mode independence. Sets up Session 3.4 (DailyBriefing wiring + Gate 1 trigger).
- **2026-05-04** PR #14 [`55325d0`] S1.5 part 2 / Session 3.2: typed Maestro task envelopes (ADR-001 D5). New `internal/brain/maestro_tasks.go` adds the typed `/v1/ai/tasks` surface alongside the existing session-based `Chat`. Five typed methods (`DailyBriefing`, `IntentClassify`, `InvoiceExtract`, `ProcurementRecommend`, `TribunalReview`) each route through one shared `runTask()` helper that handles the discriminated `{task, input}` envelope. Every response carries an embedded `CostMetadata{run_id, tokens_used, cost_cents, currency_code}` per ADR-001 D5 cost-tracking shape so callers can write a per-call billing-audit row. Composite Currency Pattern preserved everywhere monetary fields appear (invoice total, line items, vendor predicted-spend, AI run cost). Client-side validation matches existing pattern (utterance/dispute_id/material_request_id required; either document_url or text). 11 new tests cover round-trip envelope encoding, cost metadata propagation, validation rejection without server hit, and `*HTTPError` chain through the typed wrapper. Cross-repo blocking edge: Brain-side OpenAPI fixture for `/v1/ai/tasks` not yet published — contract tests deferred. Sets up Sessions 3.3 (Hub stubs / D6) and 3.4 (DailyBriefing wiring + Gate 1).
- **2026-05-04** PR #13 [`89b671e`] S1.5 part 1 / Session 3.1: brain-client resilience layer. (a) In-house circuit breaker (`internal/brain/resilience.go`, ~80 LOC) with closed→open→half-open state machine, generation-counter for stale-outcome safety, defaults 5 failures / 60s window, 30s open. No external `gobreaker` dep per `.agents/TECH_STACK.md` policy. (b) Per-method timeouts via `Config.Timeouts` (Maestro 30s, Billing 5s); caller's tighter ctx deadline wins. (c) Retry defaults retuned to plan spec: 3 attempts, 200ms base, 4× multiplier (200→800→3.2s spacing). 4xx (client error) does NOT count toward breaker threshold. New `ErrCircuitOpen` sentinel for service-layer 503 mapping. 8 new resilience tests; race-clean. Sets up Sessions 3.2 (Maestro typed envelopes / D5), 3.3 (Hub proxy stubs / D6), 3.4 (Daily Briefing wiring + Gate 1).
- **2026-05-04** PR #12 [`376eefa`] Tier 1 #3 / D7 wave 3: schedule + pipeline transition audit recording. `ScheduleService.RecalculateSchedule` writes `schedule.recalculated` audit row inside the recalc tx with `{task_count, critical_path_size, compute_ms, project_end}`. `PipelineService.AdvanceProspect` / `LoseProspect` / `transitionToPermitIssued` write `pipeline.stage_transitioned` inside the existing tx with before/after stage + probability_pct (project_id metadata for permit-issued, reason for lose). Both constructors take `AuditRecorder` (nil → noop), matching the Fleet/Procurement/Field/Budget pattern. API handlers extract `claims.Sub` via `mw.MustClaimsFromContext` and pass through. New `AuditResourceSchedule` constant. Mock services + happy-path tests assert `user_sub` propagates. Closes the audit-trail gap for the two highest-mutation surfaces (CPM recalcs + pipeline stage transitions).
- **2026-05-04** PR #11 [`e9edb96`] Tier 1 #2: slog Restricted-class PII attribute scrub via `obs.CorrelatingHandler`. `Handle` rebuilds the `slog.Record` to mask attrs whose key matches `pii.FieldClass` at Restricted (email, phone, gps_*, oidc_subject, ip_address, etc.). Group attrs recurse. `WithAttrs`-baked attrs are also scrubbed so long-lived loggers can't smuggle PII. Confidential and below pass through unchanged (vendor names, *_cents, request_id/trace_id/span_id retained for triage). 6 new test functions (~14 catalog cases). Completes the 3-leg PII-egress trio: Sentry (PR #6) + audit JSONB (PR #10) + slog (PR #11).
- **2026-05-04** PR #10 [`92231c0`] Tier 1 #1: audit-log JSONB Restricted-class PII scrub. `AuditStore.InsertAudit` now wraps Before/After/Metadata in `pii.ScrubJSON(blob, pii.Restricted)` before INSERT. Confidential-class fields (vendor, *_cents, project) intentionally preserved for investigative value. 5 unit tests + 1 integration round-trip test.
- **2026-05-04** PR #9 [`6119ab3`] CI + release workflows activated at `.github/workflows/` (5 commits: relocation, gofmt sweep across repo, Trivy action `v0.36.0` real-tag pin, Dockerfile alpine bump 3.20→3.22). All 6 CI jobs green on first activation. Plus L8 SRE audit gate added to PR description protocol.
- **2026-05-04** PR #8 [`chore/workstation-switch`] CI workflow YAMLs recovered to `docs/ci-templates/` + workstation-switch checklist added to this file.
- **2026-05-01** PR #7 [`8ea5a3e`] HANDOFF.md + NEXT_STEPS.md + CLAUDE.md refresh — cross-session continuity docs.
- **2026-05-01** PR #6 [`d71a98f`] PII classification + Sentry BeforeSend masking. `internal/pii/` package + `obs.scrubSentryEvent`. Closes the GDPR/CCPA gap on Sentry egress.
- **2026-05-01** PR #5 [`ca36fa3`] OpenTelemetry tracing — `internal/obs/tracer.go`, brain-client `otelhttp` wrap, router middleware, log-correlation (trace_id+span_id alongside request_id).
- **2026-05-01** PR #4 [`7facfce`] `cmd/buildos-fork-init` keypair generator + `docs/fork-onboarding.md`.
- **2026-05-01** PR #3 [`29ea6fe`] SecretSource abstraction (env/file/chain) + `LoadWithSource()`.
- **2026-05-01** PR #2 [`699a64d`] Sprints 1-5 + Phase F core (44 commits — domain endpoints, Brain integration, production hardening, Dockerfile, D8 build-tag hardening).

24 PRs merged under the L8 self-audit gate (PR #9 added the L8 SRE
audit gate as a second checklist applied per PR). Every landed
commit had `make audit` green + integration suite green +
govulncheck clean. PRs #9 onward also have CI green at merge time
(no longer just local audit).

## In flight

**Standalone pivot — DONE and merged.** Native-stack code, Wave 2 deletion,
docs sweep, and projects CRUD all landed in `dc78aa7` (direct push to
`origin/main`, owner-approved, no PR). `buildos-kelbrook` deployment torn
down for a clean-slate redeploy. Backend is at a clean checkpoint.

**Frontend build — web console (`web/`) Phases A–F DONE.** The Lit + Vite +
TS-strict console is built out against the native backend, monorepo-style under
`web/`. Specs in `.agents/handoff/frontend/` (FRONTEND_ARCHITECTURE,
DESIGN_SYSTEM_COMPONENTS, UX_AUTH_ONBOARDING, UX_CORE_SCREENS).
- **A** scaffold + tokens + typed API client (single-flight 401→refresh) + router/RBAC guards + CI gates (incl. composite-currency ESLint rule).
- **B** `fb-*` component library (atoms → organisms + app shell).
- **C** auth/first-run/onboarding wizard/BYOK integrations.
- **D** portfolio workspace (projects, financials, pipeline, fleet, HR).
- **E** command center (CPM Gantt, AI adjustments, procurement, feed, briefing, audit).
- **F** hardening + a11y: PII-scrubbing Sentry `beforeSend` (`src/obs/{pii,sentry}.ts`,
  mirrors `internal/pii`), density persistence, full error-taxonomy treatment tests,
  route-by-route axe sweep (Playwright + `@axe-core/playwright`, 4 public routes zero
  violations) + keyboard-focus reachability. Fixed cross-shadow accessible-name gap
  (`fb-field.wireControl` now pushes its label onto the slotted control as `aria-label`)
  and `ApiError.isTransient` (NOT_IMPLEMENTED/501 no longer retried).
  Gate green: typecheck, 158 vitest, build, eslint+prettier, 5 Playwright a11y.

**Frontend build — Flutter field app (`mobile/`) Phase G DONE.** Offline-first
field surface built monorepo-style under `mobile/` against the native backend
field contracts (caller-scoped writes: `POST /field/{progress,checkin,daily-log}`,
`GET /field/sync?since=`).
- **G1** scaffold: Flutter (3.41 / Dart 3.11), `go_router` (auth-driven redirect),
  Riverpod 3.x DI graph (`lib/providers/app_providers.dart`), GableLBM Industrial
  Dark theme (solid surfaces, AAA sunlight contrast, 56/64px touch targets),
  ARB i18n EN+ES.
- **G2** native login reusing backend contracts; tokens in `flutter_secure_storage`;
  offline launch against cached session; `dio` single-flight 401→refresh→retry
  interceptor mirroring web.
- **G3** offline core: Drift outbox table (`id, action, payload, idempotency_key,
  status, retry_count`); `connectivity_plus` FIFO drain with exponential backoff
  (base 2s × 2^n, cap 30min, maxRetries 6); UUID idempotency_key so 409 = "already
  accepted" → mark synced; 4xx validation → park failed; 5xx/network → retry;
  server-wins re-pull after drain; `FbSyncChip` passive indicator.
- **G4** field screens (bottom nav Tasks/Log/Photos/More): Tasks (priority-sorted,
  slider-to-complete + one-tap mark-done), Daily Log (camera via `image_picker` +
  GPS via `geolocator` + crew check-in), Photos, Sync Status, Profile, read-only
  Gantt (`FbGanttView`).
- **G5** push + hardening: `PushService` FCM wake-hint (degrades to no-op when
  Firebase unconfigured; no backend device-token endpoint exists, so push only
  triggers sync — not a data channel). 12 tests green (outbox-action round-trip,
  6-scenario drain matrix via scripted fake API + in-memory Drift, FbSyncChip
  widget tests). `flutter analyze` clean, `dart format` applied. New `mobile` CI
  job in `.github/workflows/ci.yml` (pub get + format check + analyze + test);
  generated `app_database.g.dart` committed so CI needs no build_runner.

**Backend-dependent E2E harness — DONE (2026-06-02).** `scripts/e2e-backend.sh`
boots a live native-auth `cmd/server` against a seeded Postgres; the `e2e` (web
Playwright: claim → wizard → portfolio) and `e2e-mobile` (Flutter offline-outbox:
queue → reconnect → drain → 409 idempotency replay) CI lanes both run against it.
Surfaced + fixed two ship-blocking native-auth bugs (subject→user resolution keyed
on the legacy `oidc_subject` column in both `field.go` and `feed_cards.go`) plus the
mobile `{data,meta}` envelope-unwrap bug. See the top "Last shipped" entry.

**Next:** OpenAPI spec generation + drift detection (NEXT_STEPS #9 — needs a
codegen dep, `swag`/`oapi-codegen`, added to `.agents/TECH_STACK.md` first, so
flag for owner sign-off before landing). The remaining E2E carryover is the
backend-dependent **golden image tests for web** + Flutter `integration_test`
(airplane-mode → queue → drain) — distinct from the now-shipped `FbSyncChip`
component goldens. The web live lane covers claim→wizard→portfolio,
recalc→CPM cascade-diff, and BYOK→AI-on capability flip. **Backup/DR is in**
(`scripts/backup-db.sh` + `restore-db.sh` + `docs/dr-runbook.md`, Tier 2 #5)
and **Flutter component goldens are in** (`mobile/test/fb_sync_chip_golden_test.dart`,
opt-in `--tags golden` — see the top "Last shipped" entry). NEXT_STEPS.md has
drift: Tier 1 #1–3 + Tier 2 #4–5 all shipped, #7 (Brain mTLS) is obsolete
post-pivot. **CI note for goldens:** the `mobile` job's Flutter is unpinned
(`channel: stable`); to make goldens a CI gate later, pin `flutter-version` and
flip the env-guard. (Workflow edits need the `workflow` OAuth scope — push from
a user clone; see the appendix.)

---

<details>
<summary>Historical Brain-era status (obsolete — pre-pivot)</summary>

**Gate 2 ratified by owner (2026-05-06).** Per
[ADR-003](./.agents/handoff/ADR-003-gate-2-ratification.md):
ADR-001 D14 (LocalBlue auto-flow) ACCEPTED unconditionally;
ADR-001 D7 (A2A receiver scope) ACCEPTED with payload-alignment
caveat (Option A — envelope + event-type list locked, per-event
payload schemas deferred until Brain payload-alignment lands).
Locked envelope mandates `org_id` as required (BuildOS continues
to fall back to `defaultOrgID` until Brain emits the field —
forward-compatible). Receiver-side tests in PRs #22 + #23 pin the
BuildOS-side decoder shape so the alignment window is safe — Brain's
emit changes won't silently regress BuildOS handling. Four
Brain-side backlog items surfaced in the cross-repo coordination
table below.

**Gate 1 ratified by owner (2026-05-05).** S1.5 Brain Client
Foundation is locked in: D5 typed Maestro envelopes
(`internal/brain/maestro_tasks.go`) and D6 Hub proxy/direct mode
(`internal/brain/hub.go`) are now stable cross-repo contracts.
Maestro-touching sprints (S2-S6) cleared to proceed. **S2 Session
5.1 shipped (PR #17)** — PROSPECT → PROJECT atomic promotion now
emits `buildos.prospect_promoted` A2A event for Brain to disseminate.
**S3 Session 7.1 shipped (PR #18)** — `ProcurementService.RecommendVendors`
calls Brain Maestro `procurement_recommend` and persists each
ranked vendor + a batch audit row inside one tx; migration 009
lands the `procurement_recommendations` table per Composite Currency
Pattern. **S3 Session 7.2 shipped (PR #19)** — new `internal/a2a`
package with typed `EmitReviewMaterialQuote` / `EmitReviewLaborBid`
methods for domain services to enqueue outbound A2A events on a
`pgx.Tx`; signing + delivery happens later in the River worker
via the already-shipped `service.A2AOutboundService`. **S3 7.2
caller wiring shipped (PR #20)** — `ProcurementService.RequestVendorReview`
is the first real consumer; opens one tx, ownership-checks, emits,
audits, returns idempotency key. `cmd/server` constructs the
`*a2a.Emitter` over the shared River insert client and threads it
through. Labor-bid wiring deferred to S6 (no labor RFQ surface
exists yet). **Vault SecretSource shipped (PR #21)** — `internal/config/vault.go`
implements the `SecretSource` interface for HashiCorp Vault KV v2;
spec format `vault://<mount>/data/<path-prefix>`; one secret per
key with `value` field; integration test round-trips against a
real `hashicorp/vault:1.18` container. First-fork cutover (Phase H)
no longer blocked on the secret-store side. **S3 Session 8.1
shipped (PR #22)** — `internal/api/a2a_test.go` gains the
idempotency-replay test (same `X-Idempotency-Key` twice → second
returns `already_processed=true` with same `feed_card_id`) and
the > 1 MiB body cap test (`413 PAYLOAD_TOO_LARGE` rather than
the generic 400). **S3 Session 8.2 shipped (PR #23)** —
`internal/service/a2a_integration_test.go` gains 6 LocalBlue
inbound tests: idempotency replay (no duplicate prospects on Brain
retry), validation gates with FULL atomic rollback (no lost leads),
optional-field nullability, source-tag boundary, and the
nil-pipelineStore guard. Both halves of the A2A inbound surface
(handler + service) are now under test. S2 Session 5.2 was already
materially shipped in production code (handlers/RBAC/tests).
**Gate 2 ratified 2026-05-06 — see above.** **S4 Session 9.1 shipped
(PR #25)** — `RecommendScheduleAdjustments` orchestrates Brain
Maestro `update_schedule`, applies recommended duration deltas via
`ScheduleStore.UpdateTask` inside the same tx as the batch
`schedule.maestro_edit` audit row, then synchronously re-runs
`ScheduleService.RecalculateSchedule` so CPM physics re-validates.
The first agent-driven CPM-edit flow is now in production code with
the ADR-003 two-event constraint preserved (this is the outbound
BuildOS → Brain call; the inbound-A2A constraint applies elsewhere).

</details>

## Blocked

Nothing blocked right now. (PR #9 cleared the workflow-activation
blocker; CI is now live on `.github/workflows/{ci,release}.yml`.)

Known follow-up surfaced by PR #9 (not blocking, queued):

- `make audit` is weaker than CI: doesn't run `gofmt -l` strictly
  and doesn't smoke-build the Dockerfile. Add both to the local
  audit so future sessions catch issues before CI does.

## Next up (prioritized — pick from the top)

**▶ NEXT: deepen the live E2E journeys.** The backend-dependent harness itself
is DONE (`scripts/e2e-backend.sh` + the `e2e` and `e2e-mobile` CI lanes; see the
top "Last shipped" entry). What remains is widening the journeys it drives:
- **Web (Playwright, live backend):** the `e2e` lane now covers first-run claim →
  6-step wizard → portfolio, recalc → CPM cascade diff + `recalculation_ms`, AND
  BYOK set → capability flips AI on (`GET /api/v1/capabilities` shipped). Web web
  journeys are caught up; remaining live-journey work is Flutter-side.
- **Flutter:** the `e2e-mobile` lane covers airplane-mode → queue → reconnect →
  outbox drain + server-side 409 idempotency replay. Still to add: golden tests
  for the offline/sync visual states (`mobile/test/` golden harness).
- Entry points: `web/tests/e2e/`, `web/playwright.config.ts`,
  `mobile/test/live/sync_live_test.dart`, `scripts/e2e-backend.sh`,
  `.github/workflows/ci.yml`.

Post-pivot backlog (the items below are complete; kept as a record):

1. ~~**Finish the docs sweep**~~ ✅ DONE (2026-06-01) — `API_CONTRACT.md`,
   `TECH_STACK.md`, `HANDOFF.md`, `docs/fork-onboarding.md` (new artifacts:
   JWT keypair + vault master key + bootstrap token; no JWKS/Brain
   registration) all rewritten for the standalone pivot;
   `.agents/handoff/ESCALATION_LOG.md` created (ESC-001 logs the pivot as a
   resolved decision).
2. ~~**`buildos-kelbrook` teardown**~~ ✅ DONE (2026-06-01, owner-confirmed) —
   the live deployment was the obsolete Brain-era build (its k8s secrets still
   carried `A2A_SIGNING_KEY`, `BRAIN_ISSUER_URL`, `BRAIN_JWKS_URL`,
   `DEFAULT_ORG_ID`, `DEV_AUTH_MODE`). `kubectl delete namespace
   buildos-kelbrook` on `do-nyc3-kelbrook-production` removed all of it (2
   server pods + 1 worker, 2 deployments, service, ingress
   `kelbrook-os.futurebuild.ai`, 2 configmaps, 4 secrets). **No PVCs** → no
   in-cluster data lost. The external managed Postgres (`DATABASE_URL`) and the
   shared nginx LB were left intact; the brain cluster (separate kube context)
   was untouched. Repo deploy artifacts already native: Dockerfile entrypoint
   reads `JWT_*_KEY_PEM` from env (no A2A materialization); `.env.example` lists
   only native vars. **Clean slate ready for native redeploy.**
   - ⚠️ Follow-ups for the redeploy operator: the managed Postgres still holds
     Brain-era data — drop/recreate or run a fresh migration if a truly clean DB
     is wanted. DNS `kelbrook-os.futurebuild.ai → 209.38.49.16` still resolves to
     the (now ruleless) LB. New secrets needed: `JWT_PRIVATE_KEY_PEM`,
     `JWT_PUBLIC_KEY_PEM`, `VAULT_MASTER_KEY`, `BUILDOS_BOOTSTRAP_TOKEN`,
     `MAIL_FROM`, `APP_BASE_URL` (via `make fork-init`).
3. ~~**Final full verify**~~ ✅ DONE (2026-06-01) on the post-pivot tree:
   `make build`, `build-prod`, `lint-migrations`, `lint-migrations-test`,
   `test`, `test-prod`, `bench-physics` (188µs/416µs — well under the
   200ms/500ms gates), `go vet`, and `test-integration` (Testcontainers
   Postgres) all PASS. Only `make lint` (golangci-lint) was skipped —
   not installed locally; CI covers it.
4. ~~**Frontend build**~~ ✅ DONE (2026-06-01, commit `9dc8d7e`) — the
   companion frontends now live in this monorepo (`web/` Lit console
   Phases A–F, `mobile/` Flutter field app Phase G), built against the
   native backend + the binding specs in `.agents/handoff/frontend/`.
   Remaining cross-stack verification tracked as the ▶ NEXT item above.

## Working agreement (L8 self-audit gate)

The owner delegated the merge gate to Claude when they said "merge
any commits should they pass your L8 quality and scope alignment
gate." This means a session can:

1. Open a branch
2. Ship work to L8 quality
3. Self-audit (build + vet + lint + test + integration + govulncheck
   + composite-currency lint + bench-physics; per-commit messages
   PR-quality; backward compat preserved or breaking change documented)
4. Open a PR with a real description
5. Merge it via `gh pr merge N --merge` if the gate passes

Don't merge if any of: tests failing, vet failing, govulncheck
flagging, integration regression, composite-currency violation, or
the change has a meaningful security implication you haven't
explicitly thought through.

When in doubt, open the PR but don't merge — let the human review.

## Things to NOT do

- **Don't add Postgres RLS** (per ADR-002). Single-tenant fork model =
  tenant isolation through deployment isolation. RLS would add 3-10%
  query overhead for zero security benefit.
- **Don't enable per-tenant rate limiting**. Per-IP is enough; one
  tenant per deployment.
- **Don't use `git push origin --force` or rewrite published history**
  on main. Force-push to feature branches only when strictly necessary.
- **Don't commit secrets**. The fork-init tool writes `private.pem` and
  `vault_master_key.txt` with mode 0600; document that they go into a
  secret store, not the repo.
- **Don't try to push `.github/workflows/*`** with the current Claude
  Code OAuth token — it doesn't have `workflow` scope. Push from a
  user clone or via the UI.
- **Don't break composite-currency invariants**. Every monetary column
  pairs `*_cents BIGINT` with `*_currency_code VARCHAR(3)`. The
  migration linter is hard CI.
- **Don't bypass D8** by adding new dev-only auth paths without the
  `//go:build !prod` tag.
- **Don't reintroduce Brain/A2A/billing.** The pivot removed them
  deliberately. Auth, AI, credentials, and email are native now.

## Cross-repo coordination (BuildOS ↔ The Brain) — OBSOLETE

> This entire section is obsolete as of the 2026-06-01 standalone pivot.
> There is no longer a Brain repo dependency: `internal/brain`,
> `internal/a2a`, `internal/a2asigner`, and the `go.mod` `replace
> ... futurebuild-brain` directive have all been removed. Auth (native
> RS256 JWTs), AI (direct Anthropic BYOK), credentials (encrypted vault),
> and transactional email (Resend) are all owned inside the fork. Retained
> only so links from older notes don't dangle.

---

## Workstation switch checklist

Use when picking BuildOS up on a fresh workstation (e.g. switching
from a travel laptop to a primary dev box). The repos themselves are
already clean — this list is for the new environment.

### One-time setup on the new workstation

```bash
# 1. Clone the repo (self-contained now — no sibling Brain checkout needed):
mkdir ~/repos && cd ~/repos
git clone https://github.com/futurebuildai/buildos.git

# 2. Toolchain:
#    - Go 1.26+        (for build + tests)
#    - Docker           (for testcontainers-backed integration tests)
#    - golangci-lint    (for `make lint`)
#    - govulncheck      `go install golang.org/x/vuln/cmd/govulncheck@latest`
#    - gh CLI           (for PR ops)
#    - make

# 3. gh auth with workflow scope from the start. This unblocks the
#    `.github/workflows/` push that the Mac session couldn't do:
gh auth login                # follow prompts; choose HTTPS or SSH
gh auth refresh -h github.com -s workflow

# 4. git identity:
git config --global user.name "Your Name"
git config --global user.email "you@example.com"

# 5. Verify everything builds + tests:
cd ~/repos/buildos
make audit
make test-integration       # spawns Postgres via testcontainers — needs Docker

# 6. Activate the CI workflows that have been waiting in docs/ci-templates/
git checkout -b ci/activate-workflows
git mv docs/ci-templates/ci.yml      .github/workflows/ci.yml
git mv docs/ci-templates/release.yml .github/workflows/release.yml
git rm docs/ci-templates/README.md
git commit -m "ci: activate CI + release workflows"
git push -u origin ci/activate-workflows
gh pr create --base main --title "ci: activate CI + release workflows"
```

### What to read first

1. [HANDOFF.md](./HANDOFF.md) (this file) — top-of-file sections give
   you 60 seconds of "what just happened, what's next."
2. [CLAUDE.md](./CLAUDE.md) — architecture, conventions, hard CI gates.
3. [.agents/handoff/NEXT_STEPS.md](./.agents/handoff/NEXT_STEPS.md) —
   pick from Tier 1 to start work.
4. [.agents/handoff/ADR-002-single-tenant-fork-model.md](./.agents/handoff/ADR-002-single-tenant-fork-model.md)
   — single-tenant per customer fork, not multi-tenant SaaS. Note the
   2026-06-01 standalone pivot supersedes the Brain-dependency parts of
   the older ADRs: BuildOS is now self-contained (native auth/AI/vault).

### Things you don't need to bring with you

- **The worktree** — every commit is on the remote.
- **Local secrets** — `.env` files are gitignored. The
  `internal/config.SecretSource` abstraction means a fresh workstation
  with its own `.env` (or no `.env`, with `CONFIG_SOURCE=file:/path`)
  works identically.
- **Local generated artifacts** — `bin/`, `bin/prod/`, `*.test`. All
  rebuildable.

### Things to NOT carry over

- Any `private.pem` from `make fork-init` runs. Those go straight into
  a customer's secret store, never into a worktree on a personal
  laptop.
- Any tokens or credentials baked into local shell history. Audit your
  `~/.bash_history` / `~/.zsh_history` before disposal if you're
  recycling the old machine.

---

## Appendix: workflow YAML (if not in worktree)

The CI + release YAMLs are tracked at
[`docs/ci-templates/`](./docs/ci-templates/) until a session with
`workflow` OAuth scope relocates them to `.github/workflows/`. See
that directory's README for activation steps. Summary of what they do:

**`ci.yml`** mounts six jobs on every PR + push to main:
- lint-migrations (5 rules) + linter regression suite
- gofmt + go vet (both default and `-tags=prod`)
- govulncheck CI-blocking (both build modes)
- unit tests + prod-mode test
- integration tests (Testcontainers + Postgres)
- bench-physics (CPM gate)
- multi-arch docker build (linux/amd64 + linux/arm64) + Trivy scan

**`.github/workflows/release.yml`** fires on tag push (`v*.*.*`):
- multi-arch image to GHCR
- cosign keyless OIDC signing (no signing key to manage)
- syft SBOM in CycloneDX + SPDX, attached via `cosign attest`
- GitHub Release with auto-changelog from `git log`
