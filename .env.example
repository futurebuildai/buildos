# FutureBuild OS — Environment Variables
# Copy to .env and fill in values.

# =============================================================================
# Database (required)
# =============================================================================
DATABASE_URL=postgres://fb_user:fb_pass@localhost:5433/futurebuild_os?sslmode=disable
DB_POOL_MAX=25
DB_POOL_MIN=5
DB_TIMEOUT=5s

# =============================================================================
# Server
# =============================================================================
PORT=8080

# =============================================================================
# Auth — FB-Brain OIDC (required in production)
# =============================================================================
BRAIN_JWKS_URL=http://localhost:8081/jwks       # Brain JWKS endpoint for JWT validation
BRAIN_ISSUER_URL=http://localhost:8081            # Brain OIDC issuer URL

# =============================================================================
# Dev only
# =============================================================================
DEV_AUTH_BYPASS=true  # Skip JWT validation for local development

# =============================================================================
# Security
# =============================================================================
CORS_ALLOWED_ORIGINS=http://localhost:3000  # Comma-separated allowed origins
RATE_LIMIT_RPS=100                          # Requests per second
RATE_LIMIT_BURST=200                        # Burst capacity

# =============================================================================
# Anthropic AI (required for AI agents + FutureShade)
# =============================================================================
ANTHROPIC_API_KEY=  # sk-ant-... Anthropic API key

# =============================================================================
# FutureShade Autonomous Execution Engine
# =============================================================================
FUTURESHADE_ENABLED=false  # Enable/disable autonomous execution engine

# =============================================================================
# A2A Cross-System Webhooks (OS -> Brain)
# =============================================================================
A2A_TARGET_URL=http://localhost:8082/api/v1/a2a/webhook  # Brain's inbound webhook endpoint
A2A_SIGNING_KEY_PATH=  # Path to RS256 private key PEM for JWS signing
